---
phase: 12-debug-live-phase-10-registration-and-proof-failures
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - contracts/script/DeployPrivacyRegistry.s.sol
  - contracts/broadcast/DeployPrivacyRegistry.s.sol/84532/run-latest.json
  - engine/src/lib/addresses.ts
  - mcp-server/src/lib/onchain.ts
  - mcp-server/src/lib/proof-generator.ts
autonomous: false
requirements: [TOOL-01, TOOL-02, ZKRQ-01]

must_haves:
  truths:
    - "Base Sepolia must point at a per-agent AgentPrivacyRegistry deployment that supports `getAgentPoseidonRoot(uint256)` and `getAgentCapabilityCommitment(uint256)` without reverting"
    - "Engine and MCP must read the same AgentPrivacyRegistry address after the redeploy/repoint so proof generation and proof verification use the same on-chain state"
    - "A fresh live `register_identity -> check_identity -> deposit_bond -> join_auction` pass must be rerun after the address correction; old onboarding state tied to the legacy deployment is not sufficient evidence"
    - "If the redeploy/repoint still does not restore the live join path, the summary must capture the exact failing step, contract address, and structured diagnostic instead of claiming the blocker is cleared"
  artifacts:
    - path: "contracts/script/DeployPrivacyRegistry.s.sol"
      provides: "Canonical deployment path for the per-agent AgentPrivacyRegistry"
      contains: "new AgentPrivacyRegistry(identityRegistry)"
    - path: "engine/src/lib/addresses.ts"
      provides: "Engine-side configured AgentPrivacyRegistry address"
      contains: "agentPrivacyRegistry"
    - path: "mcp-server/src/lib/onchain.ts"
      provides: "MCP registration-flow AgentPrivacyRegistry address"
      contains: "agentPrivacyRegistry"
    - path: "mcp-server/src/lib/proof-generator.ts"
      provides: "MCP join-proof AgentPrivacyRegistry address used for on-chain proof-state reads"
      contains: "AGENT_PRIVACY_REGISTRY"
  key_links:
    - from: "contracts/script/DeployPrivacyRegistry.s.sol"
      to: "engine/src/lib/addresses.ts"
      via: "new Base Sepolia deployment address must propagate into engine chain reads"
      pattern: "agentPrivacyRegistry"
    - from: "contracts/script/DeployPrivacyRegistry.s.sol"
      to: "mcp-server/src/lib/onchain.ts"
      via: "new Base Sepolia deployment address must propagate into MCP onboarding writes"
      pattern: "agentPrivacyRegistry"
    - from: "contracts/script/DeployPrivacyRegistry.s.sol"
      to: "mcp-server/src/lib/proof-generator.ts"
      via: "new Base Sepolia deployment address must propagate into MCP join proof-state reads"
      pattern: "AGENT_PRIVACY_REGISTRY"
---

<objective>
Close the last live Phase 12 blocker by correcting the Base Sepolia AgentPrivacyRegistry deployment and rerunning the real onboarding/join path.

Purpose: Phase 12 code fixes made the failure truthful and diagnosable, but UAT still shows that the configured Base Sepolia registry address points to a legacy global-root contract. This plan repairs the chain/config mismatch and proves the normal MCP flow works end to end against the corrected deployment.
Output: a repointed per-agent AgentPrivacyRegistry configuration plus fresh live evidence for `register_identity -> check_identity -> deposit_bond -> join_auction`.
</objective>

<execution_context>
@/Users/zengy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-UAT.md
@.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-02-SUMMARY.md

Key source files:
@contracts/src/AgentPrivacyRegistry.sol
@contracts/script/DeployPrivacyRegistry.s.sol
@contracts/broadcast/DeployPrivacyRegistry.s.sol/84532/run-latest.json
@engine/src/lib/addresses.ts
@mcp-server/src/lib/onchain.ts
@mcp-server/src/lib/proof-generator.ts

<interfaces>
From `contracts/src/AgentPrivacyRegistry.sol`:
```solidity
function getAgentPoseidonRoot(uint256 agentId) external view returns (bytes32)
function getAgentCapabilityCommitment(uint256 agentId) external view returns (bytes32)
```

From `engine/src/lib/addresses.ts`:
```typescript
export const ADDRESSES = {
  agentPrivacyRegistry: '0x...'
} as const
```

From `mcp-server/src/lib/onchain.ts`:
```typescript
export const BASE_SEPOLIA_CONTRACTS = {
  agentPrivacyRegistry: '0x...'
} as const
```
```typescript
const AGENT_PRIVACY_REGISTRY = '0x...'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy or identify the correct per-agent AgentPrivacyRegistry and repoint all consumers</name>
  <files>
    contracts/script/DeployPrivacyRegistry.s.sol
    contracts/broadcast/DeployPrivacyRegistry.s.sol/84532/run-latest.json
    engine/src/lib/addresses.ts
    mcp-server/src/lib/onchain.ts
    mcp-server/src/lib/proof-generator.ts
  </files>
  <behavior>
    - Test 1: the chosen Base Sepolia registry address responds to `getAgentPoseidonRoot(uint256)` and `getAgentCapabilityCommitment(uint256)` without reverting
    - Test 2: engine and MCP all reference the same corrected registry address after the repoint
    - Test 3: deployment notes or summary include a sanity-check command that would have caught the legacy global-root contract before live UAT
  </behavior>
  <action>
Repair the deployment/config mismatch instead of trying to code around it.

Implementation requirements:
1. Confirm whether a correct per-agent `AgentPrivacyRegistry` is already deployed on Base Sepolia. If not, deploy a fresh instance using `contracts/script/DeployPrivacyRegistry.s.sol`.
2. Record the chosen deployment in `contracts/broadcast/DeployPrivacyRegistry.s.sol/84532/run-latest.json` and capture the address used for the rest of the phase.
3. Update all hard-coded registry address consumers to the same corrected address:
   - `engine/src/lib/addresses.ts`
   - `mcp-server/src/lib/onchain.ts`
   - `mcp-server/src/lib/proof-generator.ts`
4. Add or preserve a post-deploy sanity check in the execution notes or summary that explicitly calls both per-agent getters. The next engineer should be able to verify in one command sequence that the deployed contract is the right shape.
5. Do not relax JOIN enforcement or reintroduce the legacy global-root fallback as a success path. The fix is the deployment/config truth, not another compatibility layer.
  </action>
  <verify>
    <automated>cast call &lt;new_registry_address&gt; "getAgentPoseidonRoot(uint256)(bytes32)" &lt;agent_id&gt; --rpc-url https://sepolia.base.org && cast call &lt;new_registry_address&gt; "getAgentCapabilityCommitment(uint256)(bytes32)" &lt;agent_id&gt; --rpc-url https://sepolia.base.org && rg -n "&lt;new_registry_address&gt;" /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine/src/lib/addresses.ts /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server/src/lib/onchain.ts /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server/src/lib/proof-generator.ts</automated>
  </verify>
  <done>
    - the configured Base Sepolia registry is a per-agent contract, not the legacy global-root shape
    - engine and MCP read the same corrected address
    - deployment verification includes explicit per-agent getter checks
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-onboard a fresh agent and prove the live join path against the corrected registry</name>
  <files>
    .planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-03-SUMMARY.md
  </files>
  <behavior>
    - Test 1: a fresh `register_identity` call returns a usable agent and writes a local `agent-N.json` tied to the corrected registry deployment
    - Test 2: `check_identity` returns the expected readiness snapshot for that new agent
    - Test 3: `deposit_bond` reaches `CONFIRMED` or a truthful pending state with actionable next steps
    - Test 4: `join_auction` succeeds on Base Sepolia for the freshly onboarded agent using the normal MCP auto-proof path
  </behavior>
  <action>
Rerun the real user path after the deployment fix and treat that live pass as the finish line.

Implementation requirements:
1. Use a non-throttled Base Sepolia RPC for the live rerun. Do not use the Alchemy free-tier endpoint for final evidence.
2. Create a fresh identity after the registry correction. Do not reuse the old live evidence as proof of success, because the earlier agent state was tied to the legacy deployment context.
3. Run the normal MCP lifecycle in order:
   - `register_identity`
   - `check_identity`
   - `deposit_bond`
   - `join_auction`
4. Capture the live evidence in `12-03-SUMMARY.md`: registry address, agentId, stateFilePath, tx hashes, auctionId, bond status, and JOIN acceptance details.
5. If the live join still fails, stop and record the exact failing step plus structured diagnostics in the summary rather than claiming closure.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck && cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck</automated>
  </verify>
  <done>
    - live Base Sepolia onboarding and JOIN succeed for a fresh agent using the corrected registry
    - the summary contains enough chain evidence to distinguish a real success from a partial rerun
    - if success is not reached, the remaining blocker is captured exactly
  </done>
</task>

</tasks>

<verification>
1. Confirm the corrected Base Sepolia registry supports both per-agent getters without reverting
2. Verify the same registry address appears in `engine/src/lib/addresses.ts`, `mcp-server/src/lib/onchain.ts`, and `mcp-server/src/lib/proof-generator.ts`
3. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck`
4. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck`
5. Run a fresh live Base Sepolia pass: `register_identity -> check_identity -> deposit_bond -> join_auction`
</verification>

<success_criteria>
- Base Sepolia is no longer pointed at the legacy global-root AgentPrivacyRegistry
- engine and MCP consume the corrected registry address consistently
- a fresh live agent can complete the normal onboarding and JOIN path without bypassing ZK enforcement
- the resulting summary contains enough evidence to close the Phase 12 blocker honestly
</success_criteria>

<output>
After completion, create `.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-03-SUMMARY.md`
</output>
