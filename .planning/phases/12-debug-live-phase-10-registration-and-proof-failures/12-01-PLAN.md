---
phase: 12-debug-live-phase-10-registration-and-proof-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/src/tools/register-identity.ts
  - mcp-server/test/register-identity.test.ts
autonomous: true
requirements: [TOOL-01]

must_haves:
  truths:
    - "register_identity reconciles final on-chain state, local state-file persistence, and engine-visible readiness before deciding success vs failure"
    - "If the reconciled end state is readyToParticipate=true, register_identity returns success with warning metadata instead of PRIVACY_BOOTSTRAP_FAILED or READINESS_CHECK_FAILED"
    - "If the reconciled end state is still incomplete, the tool returns actionable recovery data including agentId when known, erc8004TxHash, stateFilePath when written, and an exact nextAction"
    - "The returned readiness block reflects the same truth a follow-up check_identity call would report for that agent"
    - "Recovered-success output does not overclaim beyond the current readiness signal; it should be clear that `/verify-identity` confirms identity/privacy visibility, not that JOIN has already been proven end to end"
  artifacts:
    - path: "mcp-server/src/tools/register-identity.ts"
      provides: "Reconciled success/warning/error result contract for post-mint onboarding outcomes"
      contains: "readyToParticipate"
    - path: "mcp-server/test/register-identity.test.ts"
      provides: "Coverage for recovered-ready success and incomplete bootstrap recovery"
      contains: "warning"
  key_links:
    - from: "mcp-server/src/tools/register-identity.ts"
      to: "engine /verify-identity"
      via: "final readiness reconciliation before choosing the MCP tool result"
      pattern: "/verify-identity"
    - from: "mcp-server/src/tools/register-identity.ts"
      to: "mcp-server/src/lib/tool-response.ts"
      via: "structured success/error content with warning or recovery payloads"
      pattern: "toolSuccess|toolError"
---

<objective>
Make `register_identity` truthful after real Base Sepolia side effects have already happened.

Purpose: Phase 10 live UAT proved the tool could mint an identity, save the agent state file, and still report failure even though `check_identity` immediately showed `readyToParticipate: true`. This plan makes the tool return the reconciled truth agents can trust.
Output: a recovery-aware `register_identity` response contract that distinguishes recovered success from genuinely incomplete onboarding.
</objective>

<execution_context>
@/Users/zengy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-CONTEXT.md
@.planning/phases/10-autonomous-mcp-tools/10-UAT.md

Key source files:
@mcp-server/src/tools/register-identity.ts
@mcp-server/src/lib/tool-response.ts
@mcp-server/src/lib/onchain.ts
@mcp-server/test/register-identity.test.ts

<interfaces>
From `mcp-server/src/tools/register-identity.ts`:
```typescript
interface VerifyIdentityResponse {
  verified: boolean
  resolvedWallet: string | null
  privacyRegistered: boolean
  poseidonRoot: string | null
}

function buildReadinessSnapshot(data: VerifyIdentityResponse): {
  walletConfigured: boolean
  erc8004Registered: boolean
  privacyRegistryRegistered: boolean
  readyToParticipate: boolean
  missingSteps: string[]
  poseidonRoot: string | null
}
```

From `mcp-server/src/lib/tool-response.ts`:
```typescript
export function toolError(code: string, detail: string, suggestion: string): ToolResult
export function toolSuccess(data: Record<string, unknown>): ToolResult
```

From `mcp-server/src/lib/onchain.ts`:
```typescript
export interface IdentityRegistrationResult {
  agentId: bigint
  txHash: Hex
}

export interface PrivacyRegistrationResult {
  txHash: Hex
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile post-mint outcomes before returning register_identity</name>
  <files>
    mcp-server/src/tools/register-identity.ts
    mcp-server/test/register-identity.test.ts
  </files>
  <behavior>
    - Test 1: when privacy registration throws but a follow-up `/verify-identity` says the minted agent is verified and privacyRegistered, the tool returns success with warning metadata and `readiness.readyToParticipate=true`
    - Test 2: when readiness reconciliation still shows incomplete state, the tool returns a structured failure with `agentId`, tx hashes, `stateFilePath` when written, `readiness`, and an explicit `nextAction`
    - Test 3: the existing happy path stays flat and machine-readable with no regression in `agentId`, tx hash, state-path, or readiness fields
    - Test 4: state-file persistence or readiness failures never return success unless the reconciled end state is actually usable for participation
    - Test 5: any recovered-success warning makes clear that readiness came from `/verify-identity` and does not falsely imply the JOIN proof path has already been exercised
  </behavior>
  <action>
Enhance `mcp-server/src/tools/register-identity.ts` so post-mint exceptions no longer become the final product truth by default.

Implementation requirements:
1. Keep the current side-effect order (`register` -> `prepareOnboarding` -> persist `agent-N.json` -> privacy registration -> readiness check), but add a reconciliation helper that can run on any branch where the minted `agentId` is already known.
2. The reconciliation helper must combine:
   - completed side effects already known in-process (`erc8004TxHash`, `privacyTxHash` when known, `stateFilePath` when written)
   - a fresh `/verify-identity` postflight for the minted `agentId` + wallet
   - whether the resolved local state file actually exists
3. Use that reconciled snapshot to choose the final result:
   - if the end state is ready (`verified && privacyRegistered`) and a usable local state file exists, return `toolSuccess(...)` with the normal flat fields plus a warning payload describing the earlier mismatch
   - if the end state is still incomplete, return a structured failure that includes the recovery details and an exact `nextAction`
4. Reuse the existing structured response style; do not invent a second ad hoc shape for warnings or recovery. Warning field names are discretionary, but the top-level success/failure status must stay unambiguous for automation.
5. Recovered-success output should be explicit about what was actually proven: `/verify-identity` confirmed ERC-8004 ownership plus privacy-root visibility and the local state file exists. Do not phrase that as if JOIN itself was already verified.
6. Preserve fail-closed behavior for genuinely incomplete onboarding. The tool must never claim success unless the reconciled end state is truly usable for `deposit_bond -> join_auction`.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/register-identity.test.ts</automated>
  </verify>
  <done>
    - `register_identity` returns success when the reconciled end state is actually ready to participate, even if an intermediate step reported failure
    - incomplete states return recovery details and an explicit nextAction instead of a misleading hard failure or false success
    - recovered-success wording does not blur identity readiness with proven JOIN success
    - the happy-path response contract remains backward compatible for working callers
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck`
2. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npx vitest run test/register-identity.test.ts`
3. Repo search of `mcp-server/src/tools/register-identity.ts` shows the tool can emit a success-with-warning outcome when reconciliation proves the agent is ready
</verification>

<success_criteria>
- `register_identity` tells the truth about live onboarding outcomes instead of trusting an intermediate exception
- agents only receive recovery-oriented failures when the new identity is not actually ready for participation
- recovered-success output remains honest about the current readiness gate rather than implying JOIN was already validated
- the tool leaves downstream automation with stable fields plus enough warning/recovery detail to continue safely
</success_criteria>

<output>
After completion, create `.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-01-SUMMARY.md`
</output>
