---
phase: 15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - mcp-server/src/lib/signer.ts
  - mcp-server/src/tools/join.ts
  - mcp-server/src/tools/bid.ts
  - mcp-server/src/tools/reveal.ts
  - mcp-server/test/join.test.ts
  - mcp-server/test/bid.test.ts
  - mcp-server/test/reveal.test.ts
autonomous: true
requirements: [AKIT-02, AKIT-03]

must_haves:
  truths:
    - "JOIN/BID/REVEAL keep the same MCP tool names, EIP-712 domain/types, and proof/nullifier semantics after the supported backend lands"
    - "The selected backend can sign the current typed-data actions without changing the ZK proof contract or ERC-8004 ownership checks"
    - "When the supported backend is missing or incapable, write tools fail closed and route operators to the advanced bridge instead of silently downgrading"
    - "Write responses expose the backend/path used so supported AgentKit activity is distinguishable from the advanced bridge"
  artifacts:
    - path: "mcp-server/src/lib/signer.ts"
      provides: "Backend-aware EIP-712 signing abstraction"
      contains: "signTypedData"
    - path: "mcp-server/src/tools/join.ts"
      provides: "Supported backend JOIN path with proof/nullifier invariants preserved"
      contains: "join_auction"
    - path: "mcp-server/src/tools/bid.ts"
      provides: "Supported backend BID/BID_COMMIT path with proof invariants preserved"
      contains: "place_bid"
    - path: "mcp-server/src/tools/reveal.ts"
      provides: "Supported backend REVEAL signing path"
      contains: "reveal_bid"
  key_links:
    - from: "mcp-server/src/lib/wallet-backend.ts"
      to: "mcp-server/src/lib/signer.ts"
      via: "selected backend supplies typed-data signing capability"
      pattern: "signTypedData"
    - from: "mcp-server/src/lib/signer.ts"
      to: "mcp-server/src/tools/join.ts"
      via: "backend-aware JOIN signing"
      pattern: "Join"
    - from: "mcp-server/src/lib/signer.ts"
      to: "mcp-server/src/tools/bid.ts"
      via: "backend-aware BID/BID_COMMIT signing"
      pattern: "Bid|BidCommit"
    - from: "mcp-server/src/lib/signer.ts"
      to: "mcp-server/src/tools/reveal.ts"
      via: "backend-aware REVEAL signing"
      pattern: "Reveal"
---

<objective>
Move the action-signing side of the auction lifecycle onto the selected backend while preserving the current ZK proof and EIP-712 contracts.

Purpose: the supported path is not real until JOIN, BID, and REVEAL can sign typed-data actions through the selected wallet backend without changing nullifier derivation, proof payload handling, or ERC-8004 owner expectations.
Output: backend-aware action signing plus regression coverage for JOIN/BID/REVEAL through the supported path and the advanced bridge fallback boundary.
</objective>

<execution_context>
@/Users/zengy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004/15-CONTEXT.md
@mcp-server/AGENTS.md

Key source files:
@mcp-server/src/lib/signer.ts
@mcp-server/src/tools/join.ts
@mcp-server/src/tools/bid.ts
@mcp-server/src/tools/reveal.ts

Guardrails:
- Keep the current EIP-712 domain/types aligned with engine and agent-client
- Preserve the current proof/nullifier semantics and fail-closed readiness boundary
- Do not change the JOIN/BID proof payload contract or reintroduce no-proof fallbacks
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor the shared signer to use the selected backend without changing typed-data semantics</name>
  <files>
    mcp-server/src/lib/signer.ts
    mcp-server/test/join.test.ts
    mcp-server/test/bid.test.ts
    mcp-server/test/reveal.test.ts
  </files>
  <behavior>
    - Test 1: AgentKit/CDP-backed signing produces the same EIP-712 structures for JOIN/BID/BID_COMMIT/REVEAL as the current raw-key signer
    - Test 2: the advanced bridge still signs successfully when explicitly selected
    - Test 3: unsupported/missing backend signing capability fails clearly before action submission
  </behavior>
  <action>
Create the backend-aware signing abstraction.

Implementation requirements:
1. Refactor `mcp-server/src/lib/signer.ts` so typed-data signing comes from the selected backend rather than only `privateKeyToAccount`.
2. Preserve the current domain values, typed-data structures, nullifier derivation rules, and proof-bound JOIN semantics exactly.
3. Keep the advanced raw-key bridge available when explicitly selected.
4. Add or update signer-level regression coverage through the existing JOIN/BID/REVEAL test suites so the supported path cannot drift from the current action contract.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/join.test.ts test/bid.test.ts test/reveal.test.ts</automated>
  </verify>
  <done>
    - the selected backend signs the current typed-data actions without changing semantics
    - the advanced bridge remains intact when explicitly selected
    - missing backend signing capability fails clearly before write submission
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread backend-aware signing through JOIN, BID, and REVEAL with explicit fallback behavior</name>
  <files>
    mcp-server/src/tools/join.ts
    mcp-server/src/tools/bid.ts
    mcp-server/src/tools/reveal.ts
    mcp-server/test/join.test.ts
    mcp-server/test/bid.test.ts
    mcp-server/test/reveal.test.ts
  </files>
  <behavior>
    - Test 1: JOIN succeeds through the supported backend while preserving proof-generation and local/on-chain proof-state checks
    - Test 2: BID/BID_COMMIT succeeds through the supported backend while preserving proof generation and sealed-bid semantics
    - Test 3: REVEAL uses the selected backend without changing signature shape
    - Test 4: if the supported backend is unavailable, write tools fail closed and point operators to the advanced bridge instead of auto-downgrading silently
  </behavior>
  <action>
Integrate backend-aware signing into the action tools.

Implementation requirements:
1. Update `join_auction`, `place_bid`, and `reveal_bid` to resolve and use the selected backend for typed-data signing.
2. Preserve the existing proof-generation contract:
   - `AGENT_STATE_FILE` / compatible state remains the normal proof source
   - `proofPayload` remains the advanced override
   - local/on-chain proof-state mismatches still fail closed
3. Include backend/path information in successful write responses so operators can see whether the supported AgentKit path or the advanced bridge executed the action.
4. When the supported backend is incomplete or unavailable, fail closed with guided recovery pointing to the advanced bridge rather than silently switching modes.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/join.test.ts test/bid.test.ts test/reveal.test.ts</automated>
  </verify>
  <done>
    - JOIN/BID/REVEAL use the selected backend for signing
    - proof/nullifier invariants stay unchanged
    - fallback behavior is explicit and fail-closed instead of silent
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/join.test.ts test/bid.test.ts test/reveal.test.ts`
2. Confirm JOIN/BID/REVEAL responses expose backend/path information in the success path
3. Confirm proof-generation, nullifier derivation, and fail-closed mismatch logic remain unchanged
4. Confirm no silent downgrade to raw-key mode occurs when the supported backend is unavailable
</verification>

<success_criteria>
- The supported backend can sign the existing auction actions
- JOIN/BID/REVEAL semantics and ZK invariants remain intact
- Operators can distinguish supported AgentKit writes from the advanced bridge
- Unsupported write setups fail closed with explicit guidance
</success_criteria>

<output>
After completion, create `.planning/phases/15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004/15-03-SUMMARY.md`
</output>
