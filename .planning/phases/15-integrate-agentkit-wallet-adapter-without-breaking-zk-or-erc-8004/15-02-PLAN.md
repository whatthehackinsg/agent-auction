---
phase: 15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - mcp-server/src/lib/onchain.ts
  - mcp-server/src/lib/identity-check.ts
  - mcp-server/src/tools/identity.ts
  - mcp-server/src/tools/register-identity.ts
  - mcp-server/src/tools/bond.ts
  - mcp-server/src/tools/exits.ts
  - mcp-server/test/register-identity.test.ts
  - mcp-server/test/identity.test.ts
  - mcp-server/test/bond.test.ts
  - mcp-server/test/exits.test.ts
autonomous: true
requirements: [AKIT-02, AKIT-03]

must_haves:
  truths:
    - "Platform-managed onboarding and explicit existing-identity attachment both stay supported under the AgentKit/CDP backend"
    - "The supported path preserves the single owner-wallet rule across ERC-8004 ownership, bond/refund writes, and readiness checks"
    - "If compatible ZK state is missing for a supported write path, the tool fails closed with recovery guidance instead of pretending the runtime is ready"
    - "On-chain write tools report which backend/path they used so operators can tell supported AgentKit from the advanced bridge"
    - "Read-only identity/readiness helpers remain truthful without weakening fail-closed write behavior"
  artifacts:
    - path: "mcp-server/src/lib/onchain.ts"
      provides: "Backend-aware Base Sepolia client/write helpers"
      contains: "walletClient|writeContract"
    - path: "mcp-server/src/tools/register-identity.ts"
      provides: "Supported backend onboarding and explicit existing-identity attachment handling"
      contains: "register_identity"
    - path: "mcp-server/src/tools/bond.ts"
      provides: "Supported backend bond deposit path with owner-wallet checks preserved"
      contains: "deposit_bond"
    - path: "mcp-server/src/tools/exits.ts"
      provides: "Supported backend refund/withdraw flows with owner checks preserved"
      contains: "claim_refund|withdraw_funds"
  key_links:
    - from: "mcp-server/src/lib/wallet-backend.ts"
      to: "mcp-server/src/lib/onchain.ts"
      via: "selected backend supplies viem-compatible write capability"
      pattern: "backend"
    - from: "mcp-server/src/lib/onchain.ts"
      to: "mcp-server/src/tools/register-identity.ts"
      via: "supported backend identity mint/privacy registration writes"
      pattern: "register"
    - from: "mcp-server/src/lib/onchain.ts"
      to: "mcp-server/src/tools/bond.ts"
      via: "supported backend USDC transfer and escrow writes"
      pattern: "transfer|record"
    - from: "mcp-server/src/lib/onchain.ts"
      to: "mcp-server/src/tools/exits.ts"
      via: "supported backend refund/withdraw writes"
      pattern: "claimRefund|withdraw"
---

<objective>
Move the on-chain identity, bond, and exit flows onto the supported wallet backend while preserving the one-owner-wallet invariant and both participation entry paths.

Purpose: Phase 15 cannot stop at backend parsing. The supported AgentKit path has to actually mint or attach identities, move bond/refund funds, and preserve owner-wallet verification without regressing the advanced bridge or the current fail-closed identity behavior.
Output: backend-aware identity/on-chain tools plus regression coverage for onboarding, explicit attach, bonding, refunds, and withdrawals.
</objective>

<execution_context>
@/Users/zengy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004/15-CONTEXT.md
@mcp-server/AGENTS.md

Key source files:
@mcp-server/src/lib/onchain.ts
@mcp-server/src/lib/identity-check.ts
@mcp-server/src/tools/identity.ts
@mcp-server/src/tools/register-identity.ts
@mcp-server/src/tools/bond.ts
@mcp-server/src/tools/exits.ts

Guardrails:
- Preserve the one persistent owner-wallet rule
- Keep the advanced raw-key bridge available, but not as the supported default
- Do not silently regenerate missing ZK state; fail closed and explain recovery
- Existing-identity adoption must be explicit, not implicit guessing
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make on-chain helpers and identity readiness backend-aware without weakening owner checks</name>
  <files>
    mcp-server/src/lib/onchain.ts
    mcp-server/src/lib/identity-check.ts
    mcp-server/src/tools/identity.ts
    mcp-server/test/identity.test.ts
  </files>
  <behavior>
    - Test 1: readiness checks resolve wallet identity through the selected backend, not only raw-key envs
    - Test 2: supported-path readiness can distinguish missing compatible ZK state from missing wallet/identity setup
    - Test 3: explicit attach mode is surfaced intentionally rather than inferred silently
  </behavior>
  <action>
Refactor the shared on-chain and readiness helpers for the supported backend.

Implementation requirements:
1. Update `mcp-server/src/lib/onchain.ts` so the supported AgentKit/CDP path can produce the same viem-compatible write/read behavior used today by raw-key helpers.
2. Update identity/readiness helpers so they can work from the backend-selected wallet/identity target instead of assuming a locally derived private-key account is always available.
3. Preserve clear owner-wallet verification against ERC-8004 `ownerOf(agentId)` and keep missing compatible proof state as a fail-closed condition.
4. Add an explicit attach contract for existing identities/state. This can be config- or input-driven, but it must be deliberate rather than guessed.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/identity.test.ts</automated>
  </verify>
  <done>
    - readiness/on-chain helpers support the selected backend
    - owner-wallet checks remain intact
    - explicit attach semantics exist without implicit identity guessing
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route register_identity, deposit_bond, claim_refund, and withdraw_funds through the supported backend</name>
  <files>
    mcp-server/src/tools/register-identity.ts
    mcp-server/src/tools/bond.ts
    mcp-server/src/tools/exits.ts
    mcp-server/test/register-identity.test.ts
    mcp-server/test/bond.test.ts
    mcp-server/test/exits.test.ts
  </files>
  <behavior>
    - Test 1: register_identity works through the supported backend and reports the backend/path used
    - Test 2: explicit attach of an existing identity plus compatible state is validated deliberately and fails clearly when state is missing or mismatched
    - Test 3: deposit_bond keeps owner-wallet authorization and idempotence while using the selected backend
    - Test 4: claim_refund and withdraw_funds preserve owner checks and destination-wallet logic through the selected backend
  </behavior>
  <action>
Thread the supported backend through the on-chain write tools.

Implementation requirements:
1. Update `register_identity` so the supported AgentKit/CDP path can perform onboarding with the selected wallet backend and can also participate in the explicit attach flow without inventing a second tool family.
2. Preserve the existing `deposit_bond`, `claim_refund`, and `withdraw_funds` tool names and JSON response shape, but make them backend-aware and include backend/path reporting in successful write responses.
3. Keep the one-owner-wallet invariant intact: bond funding, refund claiming, and withdrawal authorization must still resolve against the same ERC-8004 owner wallet for the active agent.
4. When compatible proof state is missing or mismatched for the supported path, fail closed with guided recovery; do not auto-rebootstrap silently.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/register-identity.test.ts test/bond.test.ts test/exits.test.ts</automated>
  </verify>
  <done>
    - on-chain write tools work through the selected backend
    - explicit attach and platform-managed onboarding both remain supported
    - owner-wallet authorization and fail-closed state checks are preserved
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/identity.test.ts test/register-identity.test.ts test/bond.test.ts test/exits.test.ts`
2. Confirm successful write responses expose the selected backend/path without leaking secrets
3. Confirm explicit attach is deliberate and documented in code/tests, not implicit detection
4. Confirm missing/mismatched proof state still fails closed with recovery guidance
</verification>

<success_criteria>
- The supported backend can execute the on-chain identity/bond/exit lifecycle
- Both Phase 14 entry paths are preserved
- Owner-wallet and fail-closed state invariants remain intact
- Operators can see which backend path actually executed the write flow
</success_criteria>

<output>
After completion, create `.planning/phases/15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004/15-02-SUMMARY.md`
</output>
