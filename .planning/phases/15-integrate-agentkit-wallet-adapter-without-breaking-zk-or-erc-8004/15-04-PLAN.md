---
phase: 15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004
plan: 04
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - mcp-server/README.md
  - mcp-server/.env.example
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [AKIT-03, AKIT-04]

must_haves:
  truths:
    - "The supported AgentKit/CDP path is documented as the primary MCP write backend and the raw-key path remains the advanced bridge"
    - "The supported config/env contract is documented without reintroducing the old raw-key-only guidance as the default"
    - "Real sign-off evidence shows the supported backend can complete the auction participation lifecycle on Base Sepolia without breaking ERC-8004 ownership or ZK enforcement"
    - "Read-only MCP tools remain usable when the supported write path is unavailable or intentionally not configured"
  artifacts:
    - path: "mcp-server/README.md"
      provides: "Internal implementation/operator guidance for the supported AgentKit path and advanced bridge boundary"
      contains: "AgentKit|CDP|advanced bridge"
    - path: "mcp-server/.env.example"
      provides: "Supported config surface for the AgentKit/CDP backend and advanced raw-key bridge"
      contains: "CDP|AGENT_PRIVATE_KEY"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 15 completion state and concrete plan coverage"
      contains: "Phase 15"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 15 traceability and requirement completion updates"
      contains: "AKIT-"
  key_links:
    - from: "mcp-server/.env.example"
      to: "mcp-server/README.md"
      via: "documented supported config contract"
      pattern: "CDP|AgentKit"
    - from: "mcp-server/README.md"
      to: ".planning/phases/15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004/15-04-SUMMARY.md"
      via: "sign-off evidence and supported-path guidance"
      pattern: "supported|advanced"
---

<objective>
Finish the supported-path rollout with docs/env alignment and real Base Sepolia sign-off evidence.

Purpose: the adapter is not truly supported until operators can configure it consistently and there is truthful evidence that the supported AgentKit/CDP path completes the live auction lifecycle without weakening the invariants from earlier phases.
Output: internal docs/config alignment plus local and Base Sepolia sign-off evidence for the supported wallet path.
</objective>

<execution_context>
@/Users/zengy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004/15-CONTEXT.md
@mcp-server/README.md
@mcp-server/.env.example

Guardrails:
- Keep Phase 15 internal/operator-facing; do not turn this into the public playbook from Phase 16
- Sign-off must be truthful: if the supported path cannot complete the live lifecycle, record the blocker rather than overclaiming support
- Preserve read-only MCP behavior even if supported-path sign-off finds a write-only issue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the MCP README and env example to the supported AgentKit/CDP backend contract</name>
  <files>
    mcp-server/README.md
    mcp-server/.env.example
  </files>
  <action>
Document the supported backend contract for internal/operators.

Implementation requirements:
1. Update `mcp-server/README.md` so it explains:
   - the supported AgentKit/CDP path
   - the advanced raw-key bridge
   - the one-owner-wallet invariant
   - both supported entry paths (platform-managed onboarding and explicit attach)
   - the fail-closed missing-ZK-state behavior
2. Update `mcp-server/.env.example` to match the actual supported config surface after Plans 15-01 through 15-03 land.
3. Keep this internal/operator-facing. Do not expand into the public skill/playbook reserved for Phase 16.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && rg -n "AgentKit|CDP|advanced bridge|attach|register_identity|AGENT_PRIVATE_KEY" mcp-server/README.md mcp-server/.env.example</automated>
  </verify>
  <done>
    - internal docs/env describe the supported backend contract accurately
    - the advanced bridge stays clearly marked as advanced
    - Phase 16 public playbook work does not leak into this plan
  </done>
</task>

<task type="auto">
  <name>Task 2: Produce truthful supported-path sign-off evidence on Base Sepolia</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <action>
Run the supported-path sign-off and close Phase 15 truthfully.

Implementation requirements:
1. Capture local and/or targeted regression evidence showing the explicit attach path still works with the supported backend contract.
2. Run a real Base Sepolia supported-path sign-off using the AgentKit/CDP-backed wallet path. The closure gate must include:
   - wallet/provider connectivity through the supported backend
   - ERC-8004 identity ownership preserved for the active agent
   - readiness via `check_identity`
   - a real active-participation path such as `register_identity` or explicit attach, then `deposit_bond`, `join_auction`, and `place_bid`
3. If exit flows are not naturally exercised by the live run, record them as covered by targeted regression evidence or deferred with a truthful note.
4. If the supported path blocks anywhere, stop and record the real blocker instead of marking the phase complete.
5. Update roadmap/requirements/state to reflect the truthful result once the sign-off outcome is known.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npm test</automated>
  </verify>
  <done>
    - there is truthful sign-off evidence for the supported backend path
    - Phase 15 is only marked complete if the supported live path actually works
    - planning/requirements/state reflect the real outcome rather than aspirational status
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npm test`
2. Confirm `mcp-server/README.md` and `.env.example` match the actual supported backend contract
3. Confirm the sign-off evidence explicitly states whether platform-managed onboarding, explicit attach, JOIN, BID, and exits were proven live, regression-covered, or deferred
4. Confirm read-only MCP tools remain unaffected by the supported-path rollout
</verification>

<success_criteria>
- Operators have accurate internal guidance for the supported backend path
- The supported AgentKit/CDP path has truthful Base Sepolia sign-off evidence
- Phase 15 only closes if the supported path actually works end to end
- Roadmap/requirements/state are updated to reflect the real result
</success_criteria>

<output>
After completion, create `.planning/phases/15-integrate-agentkit-wallet-adapter-without-breaking-zk-or-erc-8004/15-04-SUMMARY.md`
</output>
