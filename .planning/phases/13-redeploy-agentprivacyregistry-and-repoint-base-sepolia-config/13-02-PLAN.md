---
phase: 13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - engine/src/lib/crypto.ts
  - engine/src/handlers/actions.ts
  - engine/test/actions.test.ts
  - engine/test/join-proof.test.ts
  - engine/test/bid-proof.test.ts
  - mcp-server/src/lib/proof-errors.ts
  - mcp-server/src/tools/join.ts
  - mcp-server/src/tools/bid.ts
  - mcp-server/test/join.test.ts
  - mcp-server/test/bid.test.ts
autonomous: true
requirements: [ZKRQ-01, ZKRQ-02, TOOL-02]

must_haves:
  truths:
    - "JOIN and BID converge on a shared proof-runtime outage code family when the shared Worker verifier is unavailable"
    - "`check_identity` remains an identity/privacy readiness check and is not repurposed as a proof-runtime health probe"
    - "Structured tool errors stay guided and machine-readable, while still preserving the raw runtime clue in diagnostics when it helps debugging"
    - "Ordinary proof invalidity and local/on-chain mismatch reasons remain distinguishable from a runtime outage"
    - "Regression coverage protects both the new shared runtime-outage surface and the normal proof-success path after the Worker-safe fix lands"
  artifacts:
    - path: "engine/src/handlers/actions.ts"
      provides: "Structured JOIN/BID runtime-outage mapping and fail-closed rejection contract"
      contains: "StructuredActionError|PROOF_RUNTIME_UNAVAILABLE"
    - path: "mcp-server/src/lib/proof-errors.ts"
      provides: "Shared MCP parser/mapper for structured engine proof errors"
      contains: "parseEngineStructuredError|PROOF_RUNTIME_UNAVAILABLE"
    - path: "mcp-server/src/tools/join.ts"
      provides: "JOIN tool surfacing that preserves readiness boundary and shared runtime-outage handling"
      contains: "readinessBoundaryNote"
    - path: "mcp-server/src/tools/bid.ts"
      provides: "BID tool surfacing that matches JOIN for shared proof-runtime failures"
      contains: "zkError|PROOF_RUNTIME_UNAVAILABLE"
  key_links:
    - from: "engine/src/lib/crypto.ts"
      to: "engine/src/handlers/actions.ts"
      via: "runtime-init or verify exceptions classified into stable structured action errors"
      pattern: "verification_error|proof_runtime_unavailable"
    - from: "engine/src/handlers/actions.ts"
      to: "mcp-server/src/lib/proof-errors.ts"
      via: "shared proof error code family parsed by both JOIN and BID tools"
      pattern: "PROOF_RUNTIME_UNAVAILABLE"
    - from: "mcp-server/src/lib/proof-errors.ts"
      to: "mcp-server/src/tools/join.ts"
      via: "JOIN tool consumes shared engine proof error parser"
      pattern: "parseEngineStructuredError"
    - from: "mcp-server/src/lib/proof-errors.ts"
      to: "mcp-server/src/tools/bid.ts"
      via: "BID tool consumes shared engine proof error parser"
      pattern: "parseEngineStructuredError"
---

<objective>
Align the shared JOIN/BID failure surface so proof-runtime outages are fail-closed, explicit, and consistent across engine and MCP.

Purpose: once the Worker-safe verifier exists, the phase still needs honest error surfacing for any remaining or future runtime outage. This plan makes the engine classify shared proof-runtime failures distinctly from normal invalid proofs, then makes MCP JOIN and BID expose the same code family with guided diagnostics and regression coverage.
Output: consistent engine and MCP error contracts for shared proof-runtime outages plus targeted tests that protect both JOIN and BID.
</objective>

<execution_context>
@/Users/zengy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-CONTEXT.md
@.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-02-PLAN.md
@.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-03-SUMMARY.md

Key source files:
@engine/src/lib/crypto.ts
@engine/src/handlers/actions.ts
@engine/test/actions.test.ts
@engine/test/join-proof.test.ts
@engine/test/bid-proof.test.ts
@mcp-server/src/tools/join.ts
@mcp-server/src/tools/bid.ts
@mcp-server/test/join.test.ts
@mcp-server/test/bid.test.ts

<interfaces>
From `engine/src/handlers/actions.ts`:
```typescript
interface StructuredActionErrorPayload {
  error: string
  detail: string
  suggestion: string
  reason?: string
  diagnostics?: Record<string, unknown>
}

export class StructuredActionError extends Error {
  constructor(public readonly payload: StructuredActionErrorPayload, public readonly status = 400)
}
```

From `engine/src/lib/crypto.ts`:
```typescript
export type MembershipProofReason =
  | 'valid'
  | 'missing_proof'
  | 'malformed_proof'
  | 'groth16_invalid'
  | 'registry_root_mismatch'
  | 'capability_commitment_mismatch'
  | 'verification_error'
```

From `mcp-server/src/tools/join.ts`:
```typescript
function zkError(
  code: string,
  detail: string,
  suggestion: string,
  extras?: Record<string, unknown>,
)

function parseEngineStructuredError(
  message: string,
): { error: string; detail: string; suggestion: string; reason?: string } | null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify shared proof-runtime outages distinctly from ordinary invalid proofs in the engine</name>
  <files>
    engine/src/lib/crypto.ts
    engine/src/handlers/actions.ts
    engine/test/actions.test.ts
    engine/test/join-proof.test.ts
    engine/test/bid-proof.test.ts
  </files>
  <behavior>
    - Test 1: runtime-init or shared verifier crashes classify to `proof_runtime_unavailable` instead of the generic invalid-proof branch
    - Test 2: JOIN returns a structured fail-closed `PROOF_RUNTIME_UNAVAILABLE` error with raw runtime detail preserved in diagnostics
    - Test 3: BID returns the same structured `PROOF_RUNTIME_UNAVAILABLE` code family when the shared verifier is unavailable
    - Test 4: normal invalid proofs still map to `PROOF_INVALID`, and known mismatch reasons stay specific
  </behavior>
  <action>
Make engine-side proof-runtime outages explicit and shared between membership and bid verification.

Implementation requirements:
1. Extend the proof-verification result classification in `engine/src/lib/crypto.ts` so runtime-init or shared verifier crashes can be tagged as a distinct reason such as `proof_runtime_unavailable`, while preserving the existing mismatch and invalid-proof reasons.
2. Apply the same classification pattern to both membership and bid-range proof verification. The shared runtime path from Plan 13-01 means the outage code family must cover both JOIN and BID, not only membership proofs.
3. In `engine/src/handlers/actions.ts`, convert that reason into a structured fail-closed action error:
   - `error: "PROOF_RUNTIME_UNAVAILABLE"`
   - `reason: "proof_runtime_unavailable"`
   - `detail`: human-readable summary plus the raw runtime clue in diagnostics
   - `suggestion`: next action aimed at runtime recovery, not proof regeneration alone
4. Preserve all existing behavior for `missing_proof`, `groth16_invalid`, `registry_root_mismatch`, `capability_commitment_mismatch`, and normal bid-range failures. This plan adds a new outage lane; it does not flatten the rest.
5. Keep `check_identity` unchanged. Runtime availability must continue to be discovered on the write path, not by redefining readiness.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck && npm run test -- test/actions.test.ts test/join-proof.test.ts test/bid-proof.test.ts</automated>
  </verify>
  <done>
    - engine exposes a shared fail-closed proof-runtime outage contract for JOIN and BID
    - normal invalid-proof and mismatch paths remain distinct
    - runtime clues are preserved in diagnostics instead of disappearing into generic errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align MCP JOIN/BID tool responses and regressions around the shared runtime-outage code family</name>
  <files>
    mcp-server/src/lib/proof-errors.ts
    mcp-server/src/tools/join.ts
    mcp-server/src/tools/bid.ts
    mcp-server/test/join.test.ts
    mcp-server/test/bid.test.ts
  </files>
  <behavior>
    - Test 1: `join_auction` returns `PROOF_RUNTIME_UNAVAILABLE` when the engine sends the shared runtime-outage payload, while keeping the readiness-boundary note intact
    - Test 2: `place_bid` returns the same `PROOF_RUNTIME_UNAVAILABLE` code family instead of collapsing the outage to generic `PROOF_INVALID`
    - Test 3: ordinary proof invalidity still returns the existing proof-invalid guidance
    - Test 4: successful JOIN and BID responses stay unchanged after the new parser/mapping is introduced
  </behavior>
  <action>
Make MCP JOIN and BID consume the same structured proof-runtime outage contract.

Implementation requirements:
1. Extract a small shared parser/mapper in `mcp-server/src/lib/proof-errors.ts` so JOIN and BID do not drift on engine proof-error handling.
2. Update `mcp-server/src/tools/join.ts` to route `PROOF_RUNTIME_UNAVAILABLE` through the new shared parser while preserving the existing readiness-boundary explanation that `check_identity` is narrower than successful participation.
3. Update `mcp-server/src/tools/bid.ts` to parse structured engine proof errors instead of only matching the legacy "Invalid bid range proof" string. BID must now surface the shared runtime-outage code family when the shared verifier is down.
4. Keep the tool responses JSON-stringified, machine-readable, and guided. Include the raw runtime clue under diagnostics or extras when it materially helps debugging.
5. Add focused regression coverage in `mcp-server/test/join.test.ts` and `mcp-server/test/bid.test.ts` for:
   - shared runtime outage
   - ordinary invalid proof
   - successful submission
6. Do not broaden this into a prompt/doc rewrite. Keep the scope on tool response shaping and regression safety.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/join.test.ts test/bid.test.ts</automated>
  </verify>
  <done>
    - JOIN and BID surface the same proof-runtime outage code family
    - JOIN still preserves the readiness-vs-participation boundary
    - ordinary invalid proofs remain distinguishable from runtime outages
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck && npm run test -- test/actions.test.ts test/join-proof.test.ts test/bid-proof.test.ts`
2. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/join.test.ts test/bid.test.ts`
3. Confirm `check_identity` code and behavior remain unchanged by this plan
4. Capture representative JOIN and BID runtime-outage payloads in `13-02-SUMMARY.md`, including the shared code/reason and the raw runtime clue
</verification>

<success_criteria>
- JOIN and BID share one explicit fail-closed proof-runtime outage contract across engine and MCP
- readiness checks remain separate from proof-runtime health
- normal invalid-proof and mismatch cases still surface their specific guidance
- targeted tests protect the shared runtime-outage path and the happy path from regression
</success_criteria>

<output>
After completion, create `.planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-02-SUMMARY.md`
</output>
