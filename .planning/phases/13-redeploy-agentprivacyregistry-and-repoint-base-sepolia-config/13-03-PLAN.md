---
phase: 13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - .planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-UAT.md
autonomous: false
requirements: [TOOL-01, TOOL-02, ZKRQ-01, ZKRQ-02]

must_haves:
  truths:
    - "Phase 13 does not close unless both the local Worker runtime and the deployed Worker runtime are green"
    - "Final sign-off uses fresh agent plus fresh auction evidence; reusing older agents or auctions is allowed only during debugging, not for closure"
    - "JOIN is the mandatory sign-off path; BID should be exercised on the same shared runtime when practical, but it remains follow-up evidence unless it actually fails"
    - "If one runtime still fails, the summary stays blocked and records the failing runtime, worker identifier, raw tool output, and exact next action"
    - "Contract-side redeploy/repoint work remains contingency-only and only reopens if new sign-off evidence contradicts the Phase 12-03 registry truth"
  artifacts:
    - path: ".planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-UAT.md"
      provides: "Dual-runtime sign-off checklist, evidence matrix, and blocked-state rules"
      contains: "fresh agent|fresh auction|local runtime|deployed runtime"
    - path: ".planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-03-SUMMARY.md"
      provides: "Final pass/fail evidence with agent IDs, state files, auction IDs, tx hashes, seq values, worker URL/version, and MCP outputs"
      contains: "blocked|green"
  key_links:
    - from: ".planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-UAT.md"
      to: "mcp-server JOIN/BID tools"
      via: "fresh-agent and fresh-auction verification runs through the normal MCP path"
      pattern: "register_identity|check_identity|deposit_bond|join_auction|place_bid"
    - from: ".planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-UAT.md"
      to: "engine Worker deployment"
      via: "deployed-runtime sign-off evidence"
      pattern: "workers.dev|worker version|deployment"
---

<objective>
Prove the Phase 13 fix with honest local and deployed end-to-end evidence, and keep the phase blocked unless both tracks are green.

Purpose: the runtime fix is not done when unit tests pass. This plan turns the phase done bar into an explicit dual-runtime sign-off: a full local Worker-runtime flow and a full deployed-Worker flow, each using fresh agent and fresh auction evidence, with JOIN as the mandatory gate and BID exercised on the same shared runtime.
Output: a strict `13-UAT.md` runbook plus final evidence showing either both runtimes green on the mandatory JOIN path or an honestly blocked phase with the failing runtime called out.
</objective>

<execution_context>
@/Users/zengy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-CONTEXT.md
@.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-UAT.md
@.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-03-SUMMARY.md
@.planning/phases/10-autonomous-mcp-tools/10-UAT.md

Key source files:
@mcp-server/src/tools/join.ts
@mcp-server/src/tools/bid.ts
@mcp-server/src/tools/register-identity.ts
@mcp-server/src/tools/bond.ts
@engine/src/handlers/actions.ts

<interfaces>
From `13-CONTEXT.md`:
```text
Done bar:
- final sign-off must use a fresh agent
- final sign-off should use a fresh auction
- phase stays blocked unless both a real local end-to-end proof run and a deployed-Worker end-to-end proof run are green
- final summary evidence should be full-fidelity: fresh agentId, state file path, worker version, auctionId, tx hashes, successful MCP outputs
```

From `mcp-server` tool flow:
```text
register_identity -> check_identity -> deposit_bond -> join_auction
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the strict dual-runtime sign-off runbook for Phase 13</name>
  <files>
    .planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-UAT.md
  </files>
  <behavior>
    - Test 1: the UAT doc distinguishes debug reruns from final sign-off and states that only fresh-agent/fresh-auction evidence can close the phase
    - Test 2: the UAT doc has separate local-runtime and deployed-runtime tracks with the same required MCP lifecycle centered on JOIN
    - Test 3: the UAT doc includes a blocked-state rule that keeps the phase open if either runtime fails, even if the other one passes
  </behavior>
  <action>
Author the actual sign-off runbook before attempting closure.

Implementation requirements:
1. Create `13-UAT.md` in the phase directory with two explicit tracks:
   - Local Worker runtime: local engine + local MCP path using the same Worker-side proof verification code
   - Deployed Worker runtime: live `workers.dev` engine path
2. For each track, document the required lifecycle in order:
   - `register_identity`
   - `check_identity`
   - `deposit_bond`
   - `join_auction`
3. Add a separate optional BID follow-up section for each runtime track:
   - BID should be exercised on the shared runtime when practical
   - BID evidence may be recorded as `proven`, `smoke-tested`, or `deferred`
   - the phase only closes on JOIN plus dual-runtime parity unless BID itself exposes a new failure
4. Require fresh evidence for final closure. Use a distinct fresh agent and distinct fresh OPEN auction for each runtime track so the evidence stays unambiguous and one runtime cannot contaminate the other.
5. Add an evidence checklist per runtime covering:
   - worker URL and worker version/deployment identifier
   - agentId
   - stateFilePath
   - auctionId
   - ERC-8004 tx hash
   - bond tx hash or truthful pending status
   - JOIN seq / eventHash
   - raw MCP tool output JSON for any failure
   - BID evidence status (`proven`, `smoke-tested`, or `deferred`) when applicable
6. Document the blocked-state rule plainly: if local passes and deployed fails, or deployed passes and local fails, the phase stays blocked and `13-03-SUMMARY.md` must say so explicitly.
7. Keep contract-side checks as contingency notes only. The main sign-off path should not presume another privacy-registry redeploy.
  </action>
  <verify>
    <automated>rg -n "local runtime|deployed runtime|fresh agent|fresh auction|blocked" /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/.planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-UAT.md</automated>
  </verify>
  <done>
    - `13-UAT.md` provides an executable dual-runtime runbook
    - the final done bar is explicit and impossible to overclaim
    - the blocked-state rule is documented before live evidence is gathered
    - BID follow-up evidence is clearly separated from the mandatory closure gate
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute local and deployed sign-off runs and record full-fidelity evidence honestly</name>
  <files>
    .planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-UAT.md
    .planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-03-SUMMARY.md
  </files>
  <behavior>
    - Test 1: the local Worker-runtime track completes the mandatory MCP lifecycle for a fresh agent and fresh auction with successful JOIN
    - Test 2: the deployed Worker-runtime track completes the same mandatory lifecycle for a different fresh agent and fresh auction with successful JOIN
    - Test 3: BID evidence is recorded accurately as proven, smoke-tested, or deferred
    - Test 4: if either runtime still fails, the summary records the exact failing runtime, step, raw tool output, and next action instead of claiming closure
  </behavior>
  <action>
Run the real sign-off only after Plans 13-01 and 13-02 are green.

Implementation requirements:
1. First rerun the automated proof/runtime gates from Plans 13-01 and 13-02. Do not start live sign-off until those local gates are green.
2. Execute the local Worker-runtime track using the normal MCP tool flow against a locally running engine that still verifies proofs inside the Worker runtime. Final sign-off evidence for this track must use:
   - a fresh agent
   - a fresh OPEN auction
   - the normal auto-proof path (no manual proof override)
3. Execute the deployed Worker-runtime track against the current live Worker using a second fresh agent and second fresh OPEN auction. Record the live worker URL and deployment/version identifier before or during the run.
4. For both tracks, capture full-fidelity evidence in `13-03-SUMMARY.md`: agentId, state file path, auctionId, ERC-8004 tx hash, bond tx hash, JOIN seq/eventHash, and relevant MCP output.
5. Exercise BID on the same runtime when practical, but record it honestly as one of:
   - `proven`
   - `smoke-tested`
   - `deferred`
   If BID exposes a new failure, capture it explicitly and treat it as a real blocker for the shared proof-runtime phase.
6. If one runtime still fails:
   - stop short of closure
   - mark the phase blocked in the summary
   - record the failing runtime, failing step, raw error payload, worker identifier, and next action
7. If both runtimes pass on the mandatory JOIN path, state that explicitly and keep the evidence separated by runtime so the final sign-off is auditable.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck && npm run test -- test/proof-runtime-worker.test.ts test/actions.test.ts test/join-proof.test.ts test/bid-proof.test.ts && cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/join.test.ts test/bid.test.ts</automated>
  </verify>
  <done>
    - both runtime tracks either pass with fresh evidence or fail with honest, actionable summaries
    - JOIN is proven on both runtimes before closure
    - BID evidence is recorded accurately without being silently overclaimed
    - the phase cannot be closed on partial success
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck && npm run test -- test/proof-runtime-worker.test.ts test/actions.test.ts test/join-proof.test.ts test/bid-proof.test.ts`
2. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/join.test.ts test/bid.test.ts`
3. Execute the local Worker-runtime runbook in `13-UAT.md` with a fresh agent and fresh auction
4. Execute the deployed Worker-runtime runbook in `13-UAT.md` with a second fresh agent and second fresh auction
5. Confirm `13-03-SUMMARY.md` records BID as `proven`, `smoke-tested`, or `deferred`
6. `13-03-SUMMARY.md` must either show both mandatory JOIN tracks green or explicitly mark the phase blocked with the failing runtime called out
</verification>

<success_criteria>
- the phase has an explicit dual-runtime sign-off runbook
- both local and deployed Worker-runtime JOIN flows succeed for fresh agents and fresh auctions using the normal MCP lifecycle
- BID evidence is captured honestly without silently becoming an unplanned hard gate
- if either runtime fails, the final summary preserves honest blocked status instead of overclaiming completion
</success_criteria>

<output>
After completion, create `.planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-03-SUMMARY.md`
</output>
