---
phase: 13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/lib/snarkjs-runtime.ts
  - engine/src/lib/crypto.ts
  - engine/wrangler.toml
  - engine/vitest.config.ts
  - engine/package.json
  - engine/package-lock.json
  - engine/test/proof-runtime-worker.test.ts
  - engine/test/crypto.test.ts
  - engine/test/join-proof.test.ts
  - engine/test/bid-proof.test.ts
autonomous: true
requirements: [ZKRQ-01, ZKRQ-02]

must_haves:
  truths:
    - "Cloudflare Worker proof verification stays fail-closed and remains inside the Worker runtime; this phase does not route JOIN/BID around Worker-side verification as the main fix"
    - "The shared engine verifier path must be proven under a true Worker runtime, not only under plain Node import semantics"
    - "Both `verifyMembershipProof()` and `verifyBidRangeProof()` must use the same Worker-safe loader/runtime path"
    - "The current deployed blocker `URL.createObjectURL() is not implemented` must be made reproducible locally before the final loader fix is trusted"
    - "Contract/address checks are contingency-only in this plan; do not reopen redeploy/repoint work unless the local Worker harness is green and live verification still contradicts Phase 12-03"
  artifacts:
    - path: "engine/src/lib/snarkjs-runtime.ts"
      provides: "Worker-safe snarkjs/ffjavascript loader for the shared Groth16 verifier path"
      contains: "snarkjs|ffjavascript"
    - path: "engine/src/lib/crypto.ts"
      provides: "Shared lazy-loaded membership and bid-range verification entrypoints"
      contains: "getSnarkjs|verifyMembershipProof|verifyBidRangeProof"
    - path: "engine/test/proof-runtime-worker.test.ts"
      provides: "True Worker-runtime regression coverage for membership and bid-range verification"
      contains: "URL.createObjectURL|verifyMembershipProof|verifyBidRangeProof"
  key_links:
    - from: "engine/src/lib/crypto.ts:getSnarkjs"
      to: "engine/src/lib/snarkjs-runtime.ts"
      via: "shared dynamic loader used by JOIN and BID proof verification"
      pattern: "import('./snarkjs-runtime')"
    - from: "engine/src/lib/crypto.ts:verifyMembershipProof"
      to: "engine/src/lib/snarkjs-runtime.ts"
      via: "shared Groth16 verify path used by JOIN"
      pattern: "groth16.verify"
    - from: "engine/src/lib/crypto.ts:verifyBidRangeProof"
      to: "engine/src/lib/snarkjs-runtime.ts"
      via: "shared Groth16 verify path used by BID"
      pattern: "groth16.verify"
---

<objective>
Make the shared Groth16 verifier run inside Cloudflare Workers without weakening proof enforcement.

Purpose: Phase 12-03 proved the contract/config path is already corrected; the remaining blocker is Worker-side `URL.createObjectURL()` failure during deployed proof verification. This plan turns that runtime failure into a reproducible local Worker test, fixes the engine loader/bundling path, and proves both membership and bid-range verification use the same Worker-safe runtime.
Output: a Worker-safe engine proof runtime plus regression coverage that exercises the real shared JOIN/BID verifier path under a Worker runtime.
</objective>

<execution_context>
@/Users/zengy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-CONTEXT.md
@.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-03-SUMMARY.md
@.planning/phases/12-debug-live-phase-10-registration-and-proof-failures/12-UAT.md

Key source files:
@engine/src/lib/snarkjs-runtime.ts
@engine/src/lib/crypto.ts
@engine/test/crypto.test.ts
@engine/test/join-proof.test.ts
@engine/test/bid-proof.test.ts
@engine/src/test-helpers/proof-fixtures.ts
@engine/wrangler.toml
@engine/vitest.config.ts

<interfaces>
From `engine/src/lib/snarkjs-runtime.ts`:
```typescript
import snarkjsCjs from '../../node_modules/snarkjs/build/main.cjs'

export default snarkjsCjs as typeof import('snarkjs')
```

From `engine/src/lib/crypto.ts`:
```typescript
let _snarkjs: typeof import('snarkjs') | null = null
async function getSnarkjs(): Promise<typeof import('snarkjs')>

export async function verifyMembershipProof(
  proofPayload: unknown,
  options?: VerifyMembershipOptions,
): Promise<VerifyMembershipResult>

export async function verifyBidRangeProof(
  proofPayload: unknown,
  options?: VerifyBidRangeOptions,
): Promise<VerifyBidRangeResult>
```

From `engine/src/test-helpers/proof-fixtures.ts`:
```typescript
export async function generateTestMembershipProof(
  agentId: bigint,
  treeIndex: number,
): Promise<{ proof: unknown; publicSignals: string[] }>

export async function generateTestBidRangeProof(
  agentId: bigint,
  bid: bigint,
  reservePrice: bigint,
  maxBudget: bigint,
): Promise<{ proof: unknown; publicSignals: string[] }>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce the deployed Worker verifier failure locally on the shared runtime path</name>
  <files>
    engine/test/proof-runtime-worker.test.ts
    engine/test/crypto.test.ts
    engine/test/join-proof.test.ts
    engine/test/bid-proof.test.ts
    engine/vitest.config.ts
  </files>
  <behavior>
    - Test 1: a dedicated Worker-runtime test executes `verifyMembershipProof()` with a real proof inside Miniflare/workerd and reproduces the current `URL.createObjectURL()` failure before the fix
    - Test 2: the same harness executes `verifyBidRangeProof()` through the same shared loader path
    - Test 3: Node-only crypto tests remain useful, but the phase gate explicitly distinguishes them from the new Worker-runtime regression harness
  </behavior>
  <action>
Create a true Worker-runtime reproduction harness before changing the loader.

Implementation requirements:
1. Add a dedicated Worker-facing test (or equivalent Miniflare/workerd harness) that imports the same engine verifier entrypoints the real Worker uses and runs them with real proof fixtures.
2. The harness must call the shared `verifyMembershipProof()` and `verifyBidRangeProof()` exports directly or through a minimal Worker route. Do not create a parallel verifier implementation just for tests.
3. Capture the current failure mode explicitly in the test or supporting notes so the executor can show the before/after transition. The important signal is that the runtime error matches the deployed symptom, not merely that "a test failed."
4. If the existing Vitest/Miniflare config is insufficient to trigger the workerd path, adjust `engine/vitest.config.ts` or add a targeted Worker test shim so the new harness really runs in the Worker environment.
5. Keep existing Node-side crypto tests intact as secondary coverage; they should not be deleted just because the new Worker harness becomes the primary runtime gate.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run test -- test/proof-runtime-worker.test.ts test/crypto.test.ts</automated>
  </verify>
  <done>
    - the deployed runtime failure is reproducible locally on the same shared verifier path
    - the team has a permanent regression test for Worker-runtime proof loading
    - Node-only tests are clearly separated from the Worker-runtime gate
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement a Worker-safe snarkjs loader without introducing a proof bypass</name>
  <files>
    engine/src/lib/snarkjs-runtime.ts
    engine/src/lib/crypto.ts
    engine/wrangler.toml
    engine/package.json
    engine/package-lock.json
    engine/test/proof-runtime-worker.test.ts
    engine/test/crypto.test.ts
    engine/test/join-proof.test.ts
    engine/test/bid-proof.test.ts
  </files>
  <behavior>
    - Test 1: membership proofs verify successfully inside the Worker-runtime harness after the fix
    - Test 2: bid-range proofs verify successfully inside the same Worker-runtime harness after the fix
    - Test 3: malformed proofs, invalid proofs, and missing proofs still fail closed after the loader change
    - Test 4: if the loader still throws at runtime, the result remains an invalid verification outcome rather than auto-accepting or silently skipping proof checks
  </behavior>
  <action>
Implement the actual Worker-safe runtime fix on the shared engine verifier path.

Implementation requirements:
1. Use the new harness to compare the current forced CJS entry against Worker-safe candidates and determine exactly which snarkjs/ffjavascript path avoids `URL.createObjectURL()` under workerd.
2. Update `engine/src/lib/snarkjs-runtime.ts` to select the working entry or compatibility shim. Keep the fix isolated there unless `engine/src/lib/crypto.ts` needs a small contract change to preserve lazy loading or error reporting.
3. If the chosen fix requires a targeted dependency pin, alias, or Wrangler/Vitest bundling change, apply it explicitly in `engine/package.json`, `engine/package-lock.json`, `engine/wrangler.toml`, or `engine/vitest.config.ts` and document why in comments or summary notes.
4. The same loader must continue to back both `verifyMembershipProof()` and `verifyBidRangeProof()`; do not split JOIN and BID onto divergent runtime paths.
5. Do not move verification out of the Worker, disable proof checks, or add a Node-only helper service as the main solution. The phase remains strict about Worker-side verification.
6. Keep contract-side checks contingency-only. Only reopen registry/address investigation if the local Worker harness is green and deployed verification still disagrees afterward.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck && npm run test -- test/proof-runtime-worker.test.ts test/crypto.test.ts test/join-proof.test.ts test/bid-proof.test.ts</automated>
  </verify>
  <done>
    - the shared verifier loads and runs inside a true Worker runtime
    - JOIN and BID proof verification both use the same Worker-safe runtime path
    - fail-closed proof semantics survive the runtime fix unchanged
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck`
2. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run test -- test/proof-runtime-worker.test.ts test/crypto.test.ts test/join-proof.test.ts test/bid-proof.test.ts`
3. Capture the exact before/after runtime evidence in `13-01-SUMMARY.md`, including whether `URL.createObjectURL()` was reproduced locally before the fix and which loader path ultimately passed in workerd
</verification>

<success_criteria>
- the engine can verify both membership and bid-range proofs inside Cloudflare Worker runtime semantics
- the local test suite contains an explicit Worker-runtime regression harness for the deployed failure mode
- no proof-bypass fallback or non-Worker primary architecture is introduced
- contract/config work remains deferred unless the fixed Worker runtime still contradicts live Base Sepolia evidence
</success_criteria>

<output>
After completion, create `.planning/phases/13-redeploy-agentprivacyregistry-and-repoint-base-sepolia-config/13-01-SUMMARY.md`
</output>
