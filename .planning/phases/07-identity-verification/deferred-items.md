# Deferred Items

- 2026-03-05: Pre-existing test failure remains out of scope for plan 07-01: `engine/test/bond-watcher.test.ts` (`detects transfer log and calls recordBond, then marks CONFIRMED`) still fails with `expected 1, received 0`.
