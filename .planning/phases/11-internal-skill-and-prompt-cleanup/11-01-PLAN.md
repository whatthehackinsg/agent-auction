---
phase: 11-internal-skill-and-prompt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/auction/SKILL.md
  - .claude/skills/auction/bond-management/SKILL.md
  - .claude/skills/auction/sealed-bid/SKILL.md
  - mcp-server/src/tools/identity.ts
  - mcp-server/test/identity.test.ts
  - mcp-server/README.md
  - docs/zk-fix-deployment-steps.md
autonomous: true
requirements: [SKIL-01, SKIL-02, SKIL-03]

must_haves:
  truths:
    - "The stale `.claude/skills/auction/` skill tree is removed entirely with no redirect stubs, archive copies, or active-looking leftovers"
    - "Active repo-internal guidance no longer tells readers to use `selfRegister(uint256)`, `generateProof` toggles, human-operated bond transfer as the normal path, or `post_bond` as the primary workflow"
    - "`check_identity` missing-step guidance points to the current onboarding path (`register_identity` and the post-Phase-10 ERC-8004 / privacy flow), not the retired pre-Phase-10 flow"
    - "`mcp-server/README.md` is the canonical internal landing page for the current lifecycle and includes one brief note that historical planning records may still mention removed legacy skills"
    - "Historical `.planning/**` execution records are left untouched; Phase 11 cleans only active internal surfaces"
  artifacts:
    - path: "mcp-server/src/tools/identity.ts"
      provides: "Current readiness guidance for active `check_identity` output"
      contains: "register_identity"
    - path: "mcp-server/test/identity.test.ts"
      provides: "Regression coverage for current missing-step guidance"
      contains: "check_identity"
    - path: "mcp-server/README.md"
      provides: "Canonical internal landing page plus historical-reference note"
      contains: "historical planning records"
    - path: "docs/zk-fix-deployment-steps.md"
      provides: "Active deployment guidance aligned with current automatic proof behavior"
      contains: "sealed: true"
  key_links:
    - from: "mcp-server/src/tools/identity.ts"
      to: "mcp-server/src/tools/register-identity.ts"
      via: "Missing-step guidance sends operators to the current onboarding tool instead of retired contract calls"
      pattern: "register_identity"
    - from: "mcp-server/README.md"
      to: "mcp-server/src/prompts.ts"
      via: "README remains the internal landing page while prompts stay operational helpers"
      pattern: "Prompt Templates"
    - from: "docs/zk-fix-deployment-steps.md"
      to: "mcp-server/src/tools/bid.ts"
      via: "Sealed-bid verification steps reflect the live automatic proof path instead of a removed generateProof toggle"
      pattern: "sealed: true"
---

<objective>
Remove the stale repo-internal auction skill tree and align the remaining active internal guidance with the post-Phase-13 auction lifecycle.

Purpose: the old `.claude/skills/auction/*` files still teach the pre-Phase-10 world (`selfRegister`, optional proofs, manual bond transfers, `post_bond` as the normal path), and at least one active tool surface (`check_identity`) still repeats that stale onboarding advice. This plan makes the internal guidance truthful again without drifting into the external AgentKit/playbook work reserved for Phases 14-16.
Output: removed stale skill files, cleaned active internal references, one canonical internal landing page note, and regression coverage for the active readiness guidance.
</objective>

<execution_context>
@/Users/zengy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-internal-skill-and-prompt-cleanup/11-CONTEXT.md

Key source files:
@.claude/skills/auction/SKILL.md
@.claude/skills/auction/bond-management/SKILL.md
@.claude/skills/auction/sealed-bid/SKILL.md
@mcp-server/src/tools/identity.ts
@mcp-server/README.md
@mcp-server/src/prompts.ts
@docs/zk-fix-deployment-steps.md

Guardrails:
- Keep Phase 11 internal-facing only
- Do not rewrite historical `.planning/**` execution records
- Do not create the new external agent skill/playbook here; that belongs to Phase 16
- Do not introduce the AgentKit wallet standard here; that belongs to Phases 14-15
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the stale internal auction skill tree and fix active onboarding guidance</name>
  <files>
    .claude/skills/auction/SKILL.md
    .claude/skills/auction/bond-management/SKILL.md
    .claude/skills/auction/sealed-bid/SKILL.md
    mcp-server/src/tools/identity.ts
    mcp-server/test/identity.test.ts
  </files>
  <behavior>
    - Test 1: the `.claude/skills/auction/` skill files are gone and the old path has no active guidance left in it
    - Test 2: `check_identity` no longer tells users to call `selfRegister(uint256)` or to use the legacy privacy-registry address
    - Test 3: `check_identity` missing steps now point to the current onboarding path (`register_identity` and the post-Phase-10 privacy flow)
    - Test 4: regression coverage exists for the updated readiness guidance so the stale onboarding text does not creep back in
  </behavior>
  <action>
Delete the stale repo-internal auction skill tree and align the active readiness helper with the current onboarding path.

Implementation requirements:
1. Delete these files outright:
   - `.claude/skills/auction/SKILL.md`
   - `.claude/skills/auction/bond-management/SKILL.md`
   - `.claude/skills/auction/sealed-bid/SKILL.md`
   Remove the now-empty `.claude/skills/auction/` path if nothing current remains.
2. Update `mcp-server/src/tools/identity.ts` so `check_identity` missing-step guidance:
   - no longer references `selfRegister(uint256)`
   - no longer points at the old per-agent registry address
   - points to the current onboarding path, with `register_identity` as the preferred active tool-level recovery path
   - keeps `readyToParticipate` semantics unchanged
3. Add focused regression coverage in `mcp-server/test/identity.test.ts` for the missing-step guidance and readiness output.
4. Keep this task narrow: do not create replacement skill files, redirect stubs, or public-facing guidance artifacts.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/identity.test.ts</automated>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && test ! -e .claude/skills/auction/SKILL.md && test ! -e .claude/skills/auction/bond-management/SKILL.md && test ! -e .claude/skills/auction/sealed-bid/SKILL.md</automated>
  </verify>
  <done>
    - the stale internal skill tree is removed completely
    - active `check_identity` guidance reflects the current onboarding flow
    - regression coverage protects against reintroducing `selfRegister`-style advice
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean active internal docs and establish the canonical landing note</name>
  <files>
    mcp-server/README.md
    docs/zk-fix-deployment-steps.md
  </files>
  <action>
Clean the remaining active internal guidance surfaces without touching historical planning records.

Implementation requirements:
1. Update `mcp-server/README.md` so it clearly serves as the canonical internal landing page for the current auction lifecycle.
2. Add one brief note in `mcp-server/README.md` explaining that historical `.planning/**` records may still reference removed legacy skill files because those files were part of old execution records.
3. Audit active internal guidance in `mcp-server/README.md` and `docs/zk-fix-deployment-steps.md` for stale references to:
   - `generateProof` toggles as the normal interface
   - human-operated bond transfer as the normal flow
   - `post_bond` as the primary path
   - any retired onboarding contract call that contradicts Phase 10+
4. Update the sealed-bid verification wording in `docs/zk-fix-deployment-steps.md` to match the live tool behavior after Phase 09 and Phase 13, where proof generation is automatic on the active path.
5. Do not touch old `.planning/**` files even if they still mention the removed skills.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && if rg -n "selfRegister\\(|generateProof=true|You cannot send on-chain transactions yourself" mcp-server/README.md docs/zk-fix-deployment-steps.md mcp-server/src/tools/identity.ts; then exit 1; else echo "active stale references removed"; fi</automated>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && rg -n "historical planning records" mcp-server/README.md</automated>
  </verify>
  <done>
    - active internal docs no longer contradict the current lifecycle
    - `mcp-server/README.md` is clearly the internal landing page
    - a brief note explains why historical planning records may still mention the removed skill files
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/identity.test.ts`
2. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && test ! -e .claude/skills/auction/SKILL.md && test ! -e .claude/skills/auction/bond-management/SKILL.md && test ! -e .claude/skills/auction/sealed-bid/SKILL.md`
3. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && if rg -n "selfRegister\\(|generateProof=true|You cannot send on-chain transactions yourself" mcp-server/README.md docs/zk-fix-deployment-steps.md mcp-server/src/tools/identity.ts; then exit 1; else echo "active stale references removed"; fi`
4. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && rg -n "historical planning records" mcp-server/README.md`
5. Historical `.planning/**` files remain untouched by this phase
</verification>

<success_criteria>
- the stale internal auction skill tree is gone
- active repo-internal guidance now points to the current onboarding, proof, and bond flow
- the internal canonical source is obvious without creating a new external guide
- historical execution records are preserved while active stale references are cleaned away
- Phase 11 stays internal-facing and does not absorb the public standard, AgentKit adapter, or external playbook work
</success_criteria>

<output>
After completion, create `.planning/phases/11-internal-skill-and-prompt-cleanup/11-01-SUMMARY.md`
</output>
