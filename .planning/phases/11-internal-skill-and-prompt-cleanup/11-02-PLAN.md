---
phase: 11-internal-skill-and-prompt-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - mcp-server/src/tools/identity.ts
  - mcp-server/src/tools/settlement.ts
  - mcp-server/src/prompts.ts
  - mcp-server/README.md
  - mcp-server/AGENTS.md
  - docs/zk-fix-deployment-steps.md
autonomous: true
requirements: [SKIL-02, SKIL-03]

must_haves:
  truths:
    - "Live repo-internal guidance now points agents to `register_identity` and `check_identity` rather than raw `selfRegister(uint256)` or manual privacy-bootstrap instructions"
    - "JOIN and BID guidance describes the current fail-closed mandatory ZK path: use `AGENT_STATE_FILE` for auto-generated proofs or pass an advanced `proofPayload`; no `generateProof=true` parameter or optional-proof wording remains"
    - "Bonding guidance keeps `deposit_bond` as the primary path and `post_bond` as the manual fallback only"
    - "Settlement guidance for agents points to `claim_refund` and `withdraw_funds`, not direct `claimRefund()` contract calls as the normal MCP flow"
    - "One brief live note explains that old `.planning/**` records may still mention removed skill files because they are preserved execution history"
    - "This plan remains internal-facing only and does not define the external participation standard, AgentKit wallet requirements, or the public playbook reserved for Phases 14-16"
  artifacts:
    - path: "mcp-server/src/tools/identity.ts"
      provides: "Current readiness guidance for agents using the live MCP onboarding flow"
      contains: "register_identity"
    - path: "mcp-server/src/tools/settlement.ts"
      provides: "Current post-settlement handoff guidance for agent exits"
      contains: "claim_refund"
    - path: "mcp-server/src/prompts.ts"
      provides: "Prompt templates aligned to the mandatory ZK participation flow"
      contains: "AGENT_STATE_FILE"
    - path: "mcp-server/README.md"
      provides: "Canonical internal landing page for the current participation flow plus the historical-record note"
      contains: ".planning/**"
    - path: "mcp-server/AGENTS.md"
      provides: "Updated internal agent instructions for the MCP module"
      contains: "deposit_bond"
    - path: "docs/zk-fix-deployment-steps.md"
      provides: "Internal example/runbook wording that no longer teaches `generateProof=true`"
      contains: "place_bid"
  key_links:
    - from: "mcp-server/src/tools/identity.ts"
      to: "mcp-server/src/tools/register-identity.ts"
      via: "readiness guidance must hand agents to the live MCP onboarding tool, not direct contract ABI calls"
      pattern: "register_identity"
    - from: "mcp-server/src/prompts.ts"
      to: "mcp-server/src/tools/join.ts"
      via: "prompt language must match the current fail-closed JOIN proof path"
      pattern: "AGENT_STATE_FILE"
    - from: "mcp-server/src/prompts.ts"
      to: "mcp-server/src/tools/bid.ts"
      via: "prompt language must match the current fail-closed BID proof path"
      pattern: "proofPayload"
    - from: "mcp-server/src/tools/settlement.ts"
      to: "mcp-server/src/tools/exits.ts"
      via: "settlement suggestions must hand off to the current refund and withdrawal MCP tools"
      pattern: "claim_refund"
---

<objective>
Refresh the remaining live internal prompts, examples, and help text so they all agree with the post-Phase-13 MCP lifecycle and do not teach superseded onboarding, proof, bond, or exit flows.

Purpose: deleting the old skill files is not enough if current live helper surfaces still tell agents to self-register, toggle `generateProof=true`, or call contract functions directly. This plan tightens the internal-facing canonical guidance without drifting into the public participation-standard work.
Output: active internal prompt/help/doc surfaces uniformly describe the correct ERC-8004 registration ABI path, autonomous tool names, and mandatory ZK flow.
</objective>

<execution_context>
@/Users/zengy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internal-skill-and-prompt-cleanup/11-CONTEXT.md
@.planning/phases/11-internal-skill-and-prompt-cleanup/11-01-SUMMARY.md

Live guidance surfaces to align:
@mcp-server/src/tools/identity.ts
@mcp-server/src/tools/settlement.ts
@mcp-server/src/prompts.ts
@mcp-server/README.md
@mcp-server/AGENTS.md
@docs/zk-fix-deployment-steps.md

Reference implementation files:
@mcp-server/src/tools/register-identity.ts
@mcp-server/src/tools/join.ts
@mcp-server/src/tools/bid.ts
@mcp-server/src/tools/bond.ts
@mcp-server/src/tools/exits.ts

Guardrails:
- `mcp-server/README.md` is the canonical internal landing page after cleanup
- Keep this phase internal-facing only; do not define external operator guidance, AgentKit support, or a new public skill
- Historical `.planning/**` records stay untouched; add only one brief live note explaining why they may still mention removed skill files
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stale low-level onboarding and exit instructions in live MCP helper surfaces</name>
  <files>
    mcp-server/src/tools/identity.ts
    mcp-server/src/tools/settlement.ts
    mcp-server/src/prompts.ts
  </files>
  <action>
Update the live MCP-facing helper text so it matches the current toolchain instead of older contract-level flows.

1. In `mcp-server/src/tools/identity.ts`, replace the stale `missingSteps` guidance that still references:
   - `selfRegister(uint256)`
   - manual `prepareOnboarding()` / `registerOnChain()` steps
   with current MCP guidance centered on `register_identity`, `check_identity`, and the existing readiness fields.
2. In `mcp-server/src/tools/settlement.ts`, change the cancelled/settled suggestions so agent-facing language points to `claim_refund` and `withdraw_funds` as the normal MCP exit path rather than raw `claimRefund()`.
3. In `mcp-server/src/prompts.ts`, tighten any still-implicit proof wording so the prompts explicitly match the current mandatory ZK path:
   - JOIN and BID auto-generate proofs from `AGENT_STATE_FILE` when no advanced `proofPayload` override is supplied
   - no prompt should mention `generateProof=true` or suggest an optional no-proof fallback
   - keep `deposit_bond` as primary and `post_bond` as the manual fallback only
4. Focus on the prompts that guide participation most directly: `auction_rules`, `participation_loop`, `sealed_bid_guide`, and `troubleshooting`.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npx tsc --noEmit && if rg -n "selfRegister\\(|generateProof|claimRefund\\(|registerOnChain\\(|prepareOnboarding\\(" src/tools/identity.ts src/tools/settlement.ts src/prompts.ts; then exit 1; else echo "live MCP helper text aligned"; fi && rg -n "register_identity|claim_refund|withdraw_funds|AGENT_STATE_FILE|proofPayload" src/tools/identity.ts src/tools/settlement.ts src/prompts.ts</automated>
  </verify>
  <done>
- live MCP helper text no longer teaches raw contract onboarding or refund calls as the default path
- prompt wording explicitly matches the current fail-closed ZK participation flow
- bond guidance still preserves `deposit_bond` primary and `post_bond` fallback semantics
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh internal docs and module instructions without expanding into external guidance work</name>
  <files>
    mcp-server/README.md
    mcp-server/AGENTS.md
    docs/zk-fix-deployment-steps.md
  </files>
  <action>
Bring the remaining active internal docs into alignment with the current autonomous MCP lifecycle.

1. In `mcp-server/README.md`:
   - make the current autonomous lifecycle explicit as the canonical internal flow
   - tighten proof wording so JOIN/BID are described as fail-closed proof paths using `AGENT_STATE_FILE` or advanced `proofPayload`
   - add one brief note that historical `.planning/**` records may still mention removed `.claude/skills/auction/*` files because those records are preserved execution history
2. In `mcp-server/AGENTS.md`:
   - update tool-family guidance and local rules to reflect the current tool set
   - call out `deposit_bond` as the primary bond path and `post_bond` as the manual fallback
   - keep JOIN/BID described as mandatory ZK proof paths
3. In `docs/zk-fix-deployment-steps.md`, replace the stale `generateProof: true` example with current wording that matches the post-Phase-13 tool behavior and tool names.
4. Do not add any new external operator standard, supported-stack matrix, AgentKit instructions, or public-facing skill content here.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && if rg -n "selfRegister\\(|generateProof|claimRefund\\(" mcp-server/README.md mcp-server/AGENTS.md docs/zk-fix-deployment-steps.md; then exit 1; else echo "internal docs aligned"; fi && rg -n "register_identity|deposit_bond|claim_refund|withdraw_funds|AGENT_STATE_FILE|proofPayload|\\.planning/\\*\\*" mcp-server/README.md mcp-server/AGENTS.md docs/zk-fix-deployment-steps.md</automated>
  </verify>
  <done>
- the canonical internal docs match the current MCP lifecycle and proof behavior
- one brief live note explains why historical planning records may still mention removed skills
- no external-participation or AgentKit design work leaks into this cleanup plan
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npx tsc --noEmit`
2. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && if rg -n "selfRegister\\(|generateProof|claimRefund\\(|registerOnChain\\(|prepareOnboarding\\(" mcp-server/src/tools/identity.ts mcp-server/src/tools/settlement.ts mcp-server/src/prompts.ts mcp-server/README.md mcp-server/AGENTS.md docs/zk-fix-deployment-steps.md; then exit 1; else echo "stale internal guidance removed"; fi`
3. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && rg -n "register_identity|deposit_bond|claim_refund|withdraw_funds|AGENT_STATE_FILE|proofPayload" mcp-server/src/tools/identity.ts mcp-server/src/tools/settlement.ts mcp-server/src/prompts.ts mcp-server/README.md mcp-server/AGENTS.md docs/zk-fix-deployment-steps.md`
4. Confirm the only historical-reference note is explanatory and does not attempt to rewrite `.planning/**`
5. Confirm no new files are added for external standards, AgentKit integration, or public playbook content
</verification>

<success_criteria>
- active internal prompts, examples, and help text all match the current autonomous MCP lifecycle
- ERC-8004 onboarding guidance points to `register_identity` and the current `register(string agentURI)` path rather than stale direct calls
- JOIN/BID guidance reflects the mandatory ZK flow without `generateProof=true` or optional-proof language
- exit guidance points to `claim_refund` and `withdraw_funds`
- the phase stays narrowly internal-facing
</success_criteria>

<output>
After completion, create `.planning/phases/11-internal-skill-and-prompt-cleanup/11-02-SUMMARY.md`
</output>
