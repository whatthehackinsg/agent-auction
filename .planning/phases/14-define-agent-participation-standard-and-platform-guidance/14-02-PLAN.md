---
phase: 14-define-agent-participation-standard-and-platform-guidance
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - frontend/src/lib/site-links.ts
  - frontend/src/app/participate/page.tsx
  - frontend/src/components/participate/ParticipationGuidePage.tsx
  - frontend/src/components/landing/LandingPage.tsx
  - frontend/src/components/landing/sections/CTASection.tsx
  - frontend/src/app/auctions/page.tsx
  - frontend/src/app/auctions/[id]/page.tsx
autonomous: true
requirements: [PART-03, PART-04]

must_haves:
  truths:
    - "A public `/participate` page exists as the stable, checklist-first handoff URL for human and agent operators"
    - "The public guide separates a human/operator setup track from an agent/runtime participation track while keeping one shared supported stack and capability checklist"
    - "The page states that human/operator help is bootstrap-only, that ongoing participation is agent-driven after setup, and that repeated human intervention falls back to read-only observation or the advanced bridge"
    - "The page deep-links into repo docs instead of replacing them, and it clearly labels the raw-key MCP path as the advanced bridge"
    - "Landing and auction surfaces prominently link to the setup guide using agent-facing CTA language without changing the spectator/read-only identity protections on auction pages"
    - "The frontend guide does not claim the AgentKit adapter or external skill/playbook already exists"
  artifacts:
    - path: "frontend/src/lib/site-links.ts"
      provides: "Shared participation-guide route and external documentation URLs"
      contains: "PARTICIPATION_GUIDE_PATH"
    - path: "frontend/src/app/participate/page.tsx"
      provides: "Public route entry point for the setup guide"
      exports: ["default"]
    - path: "frontend/src/components/participate/ParticipationGuidePage.tsx"
      provides: "Checklist-first guide UI with support matrix, tracks, and deep links"
      contains: "Supported|Advanced|Future"
    - path: "frontend/src/components/landing/LandingPage.tsx"
      provides: "Prominent landing-page navigation to the setup guide"
      contains: "/participate"
    - path: "frontend/src/components/landing/sections/CTASection.tsx"
      provides: "Agent-setup CTA pointing to the stable guide URL"
      contains: "agent_setup_guide"
    - path: "frontend/src/app/auctions/page.tsx"
      provides: "Auctions list entry point to the participation guide"
      contains: "/participate"
    - path: "frontend/src/app/auctions/[id]/page.tsx"
      provides: "Auction-room entry point to the participation guide without leaking participant identity"
      contains: "/participate"
  key_links:
    - from: "frontend/src/lib/site-links.ts"
      to: "frontend/src/components/landing/LandingPage.tsx"
      via: "shared handoff URL constant"
      pattern: "PARTICIPATION_GUIDE_PATH"
    - from: "frontend/src/lib/site-links.ts"
      to: "frontend/src/components/participate/ParticipationGuidePage.tsx"
      via: "shared external docs links"
      pattern: "participation-guide.md|mcp-server/README.md"
    - from: "frontend/src/components/landing/sections/CTASection.tsx"
      to: "frontend/src/app/participate/page.tsx"
      via: "agent setup CTA"
      pattern: "/participate"
    - from: "frontend/src/app/auctions/page.tsx"
      to: "frontend/src/app/participate/page.tsx"
      via: "pre-participation setup guide link"
      pattern: "/participate"
    - from: "frontend/src/app/auctions/[id]/page.tsx"
      to: "frontend/src/app/participate/page.tsx"
      via: "auction-room setup guide banner/link"
      pattern: "/participate"
---

<objective>
Ship the public frontend handoff surface for the Phase 14 participation standard and wire it into the live website surfaces operators already visit.

Purpose: the repo docs can define the standard, but external humans and agents still need a stable public URL that says "read this setup guide first" and funnels them to the detailed repo sources without guessing.
Output: a public `/participate` guide page plus prominent setup-guide links from the landing page and auction surfaces.
</objective>

<execution_context>
@/Users/zengy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-define-agent-participation-standard-and-platform-guidance/14-CONTEXT.md
@docs/participation-guide.md

Frontend surfaces:
@frontend/src/components/landing/LandingPage.tsx
@frontend/src/components/landing/sections/CTASection.tsx
@frontend/src/app/auctions/page.tsx
@frontend/src/app/auctions/[id]/page.tsx

UI constraints:
- Preserve the existing spectator/read-only product intent for auction pages
- Reuse the existing doodle / pixel UI language and primitives instead of introducing a different design system
- Keep the guide checklist-first and readable by agents
- The stable public handoff URL for this phase is `/participate`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the public `/participate` setup guide page with shared guide-link constants</name>
  <files>
    frontend/src/lib/site-links.ts
    frontend/src/app/participate/page.tsx
    frontend/src/components/participate/ParticipationGuidePage.tsx
  </files>
  <action>
Create the public participation/setup route and make it the canonical frontend handoff surface for Phase 14.

1. Add `frontend/src/lib/site-links.ts` with reusable constants for:
   - `PARTICIPATION_GUIDE_PATH = "/participate"`
   - external GitHub/doc links needed by the page (canonical participation guide, MCP README, relevant repo entry points)
2. Create `frontend/src/app/participate/page.tsx` as the route entry point. Keep it lightweight and delegate the real layout to a dedicated component.
3. Create `frontend/src/components/participate/ParticipationGuidePage.tsx` using existing frontend primitives and styling patterns. The page must include:
   - a short statement that this is the agent setup guide / handoff page
   - the `Supported` / `Advanced` / `Future` matrix from Plan 14-01
   - the active-participant wallet capability checklist
   - required assets and concise config-input checklist
   - two explicit tracks:
     - human/operator setup
     - agent/runtime participation
   - an explicit handoff boundary showing that the human/operator track ends after bootstrap setup, funding, or initial launch and that participation is agent-driven afterward
   - fallback language for read-only observation or the advanced bridge
   - deep links to `docs/participation-guide.md` and `mcp-server/README.md`
   - a short copyable handoff block so a human can tell an agent to start with this page
4. Keep the page honest about phase boundaries:
   - supported stack target is `AgentKit + CDP Server Wallet`
   - current raw-key MCP flow is the advanced bridge
   - do not claim the adapter or external skill already exists
5. Keep it static/checklist-first. Do not add live engine fetches, wallet connectors, or on-page onboarding automation in this phase.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && rg -n "PARTICIPATION_GUIDE_PATH|/participate|Supported|Advanced|Future|agent setup guide|read this setup guide first|AgentKit \\+ CDP Server Wallet|advanced bridge|bootstrap-only|agent-driven" frontend/src/lib/site-links.ts frontend/src/app/participate/page.tsx frontend/src/components/participate/ParticipationGuidePage.tsx</automated>
  </verify>
  <done>
    - `/participate` exists as a stable public handoff route
    - the guide is checklist-first, split into human/operator and agent/runtime tracks, and deep-links into repo docs
    - the page makes the bootstrap-only human-help boundary explicit and hands runtime participation to the agent track after setup
    - shared route/doc URL constants exist so the handoff links are not scattered as magic strings
  </done>
</task>

<task type="auto">
  <name>Task 2: Add prominent setup-guide links to the landing and auction surfaces</name>
  <files>
    frontend/src/components/landing/LandingPage.tsx
    frontend/src/components/landing/sections/CTASection.tsx
    frontend/src/app/auctions/page.tsx
    frontend/src/app/auctions/[id]/page.tsx
  </files>
  <action>
Wire the public setup guide into the existing frontend entry points operators already use.

1. In `LandingPage.tsx`:
   - add a prominent navigation/menu entry or button for the setup guide using the shared route constant
   - keep the current landing layout intact while making the guide discoverable from the top-level experience
2. In `CTASection.tsx`:
   - add or promote a clear agent-facing CTA such as `[ agent_setup_guide ]`
   - make the copy explicit that this is the page to read before attempting active participation
3. In `frontend/src/app/auctions/page.tsx`:
   - add a compact pre-participation banner/panel/button linking to `/participate`
   - keep the auctions page spectator-first; do not add wallet-specific or identity-revealing UI
4. In `frontend/src/app/auctions/[id]/page.tsx`:
   - add a compact operator/setup link or panel that points to `/participate`
   - place it where an operator can find it without disrupting live-room readability
   - preserve all existing privacy rules: no new identity, wallet, or bid-history exposure
5. Reuse the shared route constant and existing pixel/doodle components where possible. Do not add a second competing setup destination.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/frontend && npm run lint && npm run build && cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && rg -n "/participate|agent_setup_guide|setup guide" frontend/src/components/landing/LandingPage.tsx frontend/src/components/landing/sections/CTASection.tsx frontend/src/app/auctions/page.tsx frontend/src/app/auctions/[id]/page.tsx</automated>
  </verify>
  <done>
    - landing and auction surfaces all link to the same `/participate` handoff page
    - the CTA language clearly signals that the guide is for agent setup/participation preparation
    - auction pages remain spectator-safe and privacy-preserving
  </done>
</task>

</tasks>

<verification>
1. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/frontend && npm run lint && npm run build`
2. `cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design && rg -n "PARTICIPATION_GUIDE_PATH|/participate|agent setup guide|read this setup guide first|AgentKit \\+ CDP Server Wallet|advanced bridge|bootstrap-only|agent-driven after setup" frontend/src/lib/site-links.ts frontend/src/app/participate/page.tsx frontend/src/components/participate/ParticipationGuidePage.tsx frontend/src/components/landing/LandingPage.tsx frontend/src/components/landing/sections/CTASection.tsx frontend/src/app/auctions/page.tsx frontend/src/app/auctions/[id]/page.tsx`
3. Confirm the page deep-links to repo docs instead of duplicating every implementation detail
4. Confirm auction-page additions do not introduce participant identity, wallet, or per-agent bid-history exposure
5. Confirm no wallet connector, AgentKit adapter, or external skill installation flow is added in this phase
</verification>

<success_criteria>
- A public `/participate` route exists as the stable agent setup guide handoff URL
- The page mirrors the Phase 14 standard with checklist-first content and deep links into repo docs
- Landing and auction surfaces prominently point operators to the setup guide
- The frontend work stays guidance-only and preserves the spectator/privacy model
</success_criteria>

<output>
After completion, create `.planning/phases/14-define-agent-participation-standard-and-platform-guidance/14-02-SUMMARY.md`
</output>
