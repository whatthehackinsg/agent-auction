---
phase: 10-autonomous-mcp-tools
plan: 04
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - mcp-server/src/prompts.ts
  - mcp-server/README.md
  - mcp-server/.env.example
autonomous: true
requirements: []

must_haves:
  truths:
    - "MCP prompts describe register_identity, deposit_bond, join(ZK), bid(ZK), claim_refund, and withdraw_funds as the primary autonomous lifecycle"
    - "Prompts and README no longer tell agents to ask humans for bond transfers or to call claimRefund directly as the normal path"
    - "post_bond remains documented only as an advanced/manual fallback"
    - "Phase 10 documentation stops at MCP server prompts/README and does not rewrite standalone auction skill docs reserved for Phase 11"
  artifacts:
    - path: "mcp-server/src/prompts.ts"
      provides: "Autonomous tool guidance embedded in MCP prompts"
      contains: "register_identity"
    - path: "mcp-server/README.md"
      provides: "Updated tool list, environment variables, and autonomous lifecycle documentation"
      contains: "deposit_bond"
    - path: "mcp-server/.env.example"
      provides: "Final environment surface for on-chain MCP tools"
      contains: "BOND_FUNDING_PRIVATE_KEY"
  key_links:
    - from: "mcp-server/src/prompts.ts"
      to: "mcp-server/src/tools/bond.ts"
      via: "deposit_bond primary path with post_bond fallback"
      pattern: "deposit_bond"
    - from: "mcp-server/README.md"
      to: "mcp-server/src/tools/funds.ts"
      via: "claim_refund and withdraw_funds usage documentation"
      pattern: "withdraw_funds"
---

<objective>
Refresh the in-server guidance and docs so agents are instructed to use the new autonomous Phase 10 tools in the right order.

Purpose: the tool surface is only half the job. MCP prompts and README are still teaching the old human-assisted bond/refund path, so this final plan aligns the guidance with the implemented lifecycle without stepping into the Phase 11 skill rewrite.
Output: updated prompts, README, and final env documentation for the autonomous MCP loop.
</objective>

<execution_context>
@/Users/zengy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-autonomous-mcp-tools/10-CONTEXT.md
@.planning/phases/10-autonomous-mcp-tools/10-01-SUMMARY.md
@.planning/phases/10-autonomous-mcp-tools/10-02-SUMMARY.md
@.planning/phases/10-autonomous-mcp-tools/10-03-SUMMARY.md

Key source files:
@mcp-server/src/prompts.ts
@mcp-server/README.md
@mcp-server/.env.example

Guardrails:
- Keep Phase 10 guidance inside the MCP server module only
- Do not rewrite `.claude/skills/auction` or any other skill docs here; that belongs to Phase 11
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MCP prompts to teach the autonomous Phase 10 lifecycle</name>
  <files>
    mcp-server/src/prompts.ts
  </files>
  <action>
Refresh the existing prompt templates so they match the new tool surface.

1. Update `auction_rules` so onboarding starts with `register_identity` plus `check_identity`, not raw contract calls.
2. Update `participation_loop` so the normal path is:
   `register_identity -> check_identity -> discover_auctions -> get_auction_details -> deposit_bond -> join_auction -> place_bid -> claim_refund/withdraw_funds`
3. Update `bonding_walkthrough` so `deposit_bond` is the default end-to-end path and `post_bond` is only mentioned as an advanced/manual recovery option.
4. Update post-auction guidance so:
   - losers call `claim_refund` then `withdraw_funds`
   - winners call `withdraw_funds` after settlement
   - cancelled auctions point to `claim_refund`
5. Update `troubleshooting` to reference the new tool names and Phase 10 error cases (funding wallet mismatch, nothing to withdraw, refund not ready, etc.).
6. Remove human-operator phrasing such as “ask your human operator” from the normal workflow text.
7. Keep read-side privacy wording accurate: write tools can use explicit `agentId`, but read-side outputs remain masked or nullifier-based.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npx tsc --noEmit && rg -n "register_identity|deposit_bond|claim_refund|withdraw_funds" src/prompts.ts</automated>
  </verify>
  <done>
- Prompt templates describe the new autonomous lifecycle in the right order
- Human-assisted bond/refund instructions are removed from the primary path
- Privacy wording remains consistent with Phase 08
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh README and env docs without touching Phase 11 skill files</name>
  <files>
    mcp-server/README.md
    mcp-server/.env.example
  </files>
  <action>
Update MCP-server-local documentation to match the final Phase 10 tool surface.

1. In `mcp-server/README.md`:
   - expand the tool table to include `register_identity`, `deposit_bond`, `claim_refund`, and `withdraw_funds`
   - update the environment variable section with `BASE_SEPOLIA_RPC` and optional `BOND_FUNDING_PRIVATE_KEY`
   - update the usage flow so it shows the autonomous lifecycle, not the manual tx-hash handoff flow
   - keep `post_bond` documented only as an advanced/manual fallback

2. In `.env.example`, make sure the final comments align with the implemented behavior:
   - `BASE_SEPOLIA_RPC` is required for on-chain tools
   - `BOND_FUNDING_PRIVATE_KEY` is optional and only used when the bond funding wallet is selected
   - wording does not imply a hosted metadata URL or a human-only workflow

3. Explicitly do not edit `.claude/skills/auction` or any other standalone skill doc in this plan.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npx tsc --noEmit && rg -n "register_identity|deposit_bond|claim_refund|withdraw_funds|BOND_FUNDING_PRIVATE_KEY" README.md .env.example</automated>
  </verify>
  <done>
- README matches the final Phase 10 tool set
- `.env.example` reflects the real config surface for autonomous on-chain tools
- Phase 11 skill rewrite scope is preserved
  </done>
</task>

</tasks>

<verification>
1. `cd mcp-server && npx tsc --noEmit` passes
2. `cd mcp-server && rg -n "register_identity|deposit_bond|claim_refund|withdraw_funds" src/prompts.ts README.md .env.example` finds all new tools in docs/prompt surfaces
3. `cd mcp-server && if rg -n "ask your human operator|call claimRefund\\(\\) directly" src/prompts.ts README.md; then exit 1; else echo "old manual phrasing removed"; fi` confirms the old manual phrasing is gone
4. No `.claude/skills/*` files are modified by this plan
</verification>

<success_criteria>
- MCP prompt guidance and README both teach the new autonomous lifecycle correctly
- The old human-assisted bond/refund path is no longer presented as the primary workflow
- Phase 10 documentation stays within the MCP server boundary so Phase 11 can handle the broader skill rewrite cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-autonomous-mcp-tools/10-04-SUMMARY.md`
</output>
