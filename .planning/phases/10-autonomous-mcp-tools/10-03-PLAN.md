---
phase: 10-autonomous-mcp-tools
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - mcp-server/src/tools/exits.ts
  - mcp-server/src/index.ts
  - mcp-server/test/exits.test.ts
autonomous: true
requirements: [TOOL-03, TOOL-04]

must_haves:
  truths:
    - "claim_refund preflights escrow eligibility and returns a clear idempotent outcome for settled or cancelled auctions"
    - "claim_refund success gives a strong handoff: it tells the agent how much is now withdrawable and points directly to withdraw_funds"
    - "withdraw_funds preflights owner authorization, withdrawable balance, and designated wallet before sending a transaction"
    - "withdraw_funds returns a full summary with tx hash, amount, destination wallet, agentId, and remaining-balance state"
  artifacts:
    - path: "mcp-server/src/tools/exits.ts"
      provides: "claim_refund and withdraw_funds MCP tools"
      contains: "claim_refund"
    - path: "mcp-server/test/exits.test.ts"
      provides: "Coverage for refund eligibility, idempotency, and withdraw summaries"
      contains: "withdraw_funds"
  key_links:
    - from: "mcp-server/src/tools/exits.ts"
      to: "mcp-server/src/lib/onchain.ts"
      via: "AuctionEscrow claimRefund/withdraw plus balance preflights"
      pattern: "withdraw"
    - from: "mcp-server/src/tools/exits.ts"
      to: "AuctionEscrow withdrawable/getDesignatedWallet/ownerOf"
      via: "balance-focused preflight and authorization summary"
      pattern: "withdrawable"
---

<objective>
Finish the autonomous lifecycle by adding refund and withdrawal tools with the preflights and handoffs locked during discuss-phase.

Purpose: After agents can register and bond autonomously, they still need the last mile of settlement exits. This plan makes refunds and withdrawals usable from MCP with clear preflights and handoffs.
Output: new exit tools and exit tests, ready for the final docs/prompt pass in Plan 04.
</objective>

<execution_context>
@/Users/zengy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-autonomous-mcp-tools/10-CONTEXT.md
@.planning/phases/10-autonomous-mcp-tools/10-02-SUMMARY.md

Key source files:
@contracts/src/AuctionEscrow.sol
@agent-client/src/auction.ts
@mcp-server/src/prompts.ts
@mcp-server/README.md
@engine/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement claim_refund with eligibility preflight and strong handoff</name>
  <files>
    mcp-server/src/tools/exits.ts
    mcp-server/src/index.ts
    mcp-server/test/exits.test.ts
  </files>
  <behavior>
    - Test 1: claim_refund returns a preflight error when the auction is still OPEN/CLOSED and refund is not yet claimable
    - Test 2: for SETTLED loser or CANCELLED auction paths, claim_refund submits the transaction and returns the now-withdrawable amount plus next-step guidance
    - Test 3: if refund has already been claimed or nothing remains claimable, the tool returns a clear idempotent state instead of an opaque failure
    - Test 4: the response includes target agentId, auctionId, tx hash when applicable, and an explicit next action to call withdraw_funds
  </behavior>
  <action>
Create `mcp-server/src/tools/exits.ts` (or equivalent registration module) and wire it into `mcp-server/src/index.ts`.

For `claim_refund`:
1. Accept explicit `agentId` and `auctionId` inputs.
2. Preflight on-chain before sending:
   - auction state is `SETTLED` or `CANCELLED`
   - current winner / refund exclusion state when settled
   - current withdrawable balance and bond/refund state where readable
3. If the call is eligible, send `AuctionEscrow.claimRefund(auctionId, agentId)`.
4. After success, re-read withdrawable balance and designated wallet so the response can hand off directly to `withdraw_funds`.
5. Handle already-claimed / no-bond paths as idempotent outcomes with actionable messaging rather than raw low-level reverts.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/exits.test.ts</automated>
  </verify>
  <done>
- claim_refund is available from MCP
- refund eligibility is checked before sending a transaction
- successful claim_refund responses include newly withdrawable amount and explicit next-step guidance
- duplicate or ineligible refund paths are clear and actionable
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement withdraw_funds with balance-focused preflight</name>
  <files>
    mcp-server/src/tools/exits.ts
    mcp-server/test/exits.test.ts
  </files>
  <behavior>
    - Test 1: withdraw_funds reads withdrawable balance and designated wallet before sending and returns a no-op result when nothing is withdrawable
    - Test 2: withdraw_funds enforces the owner/admin authorization expectations from AuctionEscrow.withdraw(agentId)
    - Test 3: successful withdraw_funds returns tx hash, amount, destination wallet, agentId, and remaining balance state
  </behavior>
  <action>
Extend the same exit module with `withdraw_funds`.

Implementation requirements:
1. Use the shared on-chain helper layer from Plan 10-01 for:
   - `withdrawable(agentId)`
   - `getDesignatedWallet(agentId)`
   - `ownerOf(agentId)` for authorization context
2. Implement `withdraw_funds(agentId)` so it:
   - preflights and explains current balance + destination
   - calls `AuctionEscrow.withdraw(agentId)` only when funds are available
   - returns a full success summary with tx hash, amount, destination wallet, agentId, and remaining balance
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npm run typecheck && npx vitest run test/exits.test.ts</automated>
  </verify>
  <done>
- withdraw_funds is available from MCP with a balance-focused preflight
- success output includes the full withdrawal summary the user requested
  </done>
</task>

</tasks>

<verification>
1. `cd mcp-server && npm run typecheck`
2. `cd mcp-server && npx vitest run test/exits.test.ts`
3. `rg -n "claim_refund|withdraw_funds" mcp-server/src/tools/exits.ts`
4. `rg -n "withdrawable|getDesignatedWallet|ownerOf" mcp-server/src/lib/onchain.ts && rg -n "claimRefund|withdraw\\(" mcp-server/src/tools/exits.ts`
</verification>

<success_criteria>
- Non-winning agents can claim refunds from MCP with clear preflight guidance
- Agents can withdraw settled/refunded balances from MCP without manual contract calls
</success_criteria>

<output>
After completion, create `.planning/phases/10-autonomous-mcp-tools/10-03-SUMMARY.md`
</output>
