---
phase: 09-zk-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/auction-room.ts
  - engine/src/handlers/actions.ts
  - mcp-server/src/tools/join.ts
  - mcp-server/src/tools/bid.ts
  - mcp-server/src/tools/identity.ts
  - mcp-server/src/lib/identity-check.ts
  - mcp-server/.env.example
autonomous: true
requirements: [ZKRQ-01, ZKRQ-02, ZKRQ-03, ZKRQ-04]

must_haves:
  truths:
    - "join_auction auto-generates ZK proof when AGENT_STATE_FILE is set (no generateProof param)"
    - "place_bid auto-generates ZK proof when AGENT_STATE_FILE is set (no generateProof param)"
    - "check_identity returns single readyToParticipate flag (no readyForZkProofs)"
    - "ENGINE_REQUIRE_PROOFS defaults to true when unset"
    - ".env.example shows AGENT_STATE_FILE and BASE_SEPOLIA_RPC as REQUIRED"
  artifacts:
    - path: "mcp-server/src/tools/join.ts"
      provides: "Auto-generate proof on JOIN, no generateProof param"
      contains: "AGENT_STATE_FILE"
    - path: "mcp-server/src/tools/bid.ts"
      provides: "Auto-generate proof on BID, no generateProof param"
      contains: "AGENT_STATE_FILE"
    - path: "mcp-server/src/tools/identity.ts"
      provides: "Unified readyToParticipate flag"
      contains: "readyToParticipate"
    - path: "engine/src/auction-room.ts"
      provides: "Default-true ENGINE_REQUIRE_PROOFS"
      contains: "!== 'false'"
    - path: "mcp-server/.env.example"
      provides: "Required env var documentation"
      contains: "# Required"
  key_links:
    - from: "mcp-server/src/tools/join.ts"
      to: "mcp-server/src/lib/proof-generator.ts"
      via: "auto proof generation when AGENT_STATE_FILE set"
      pattern: "generateMembershipProof"
    - from: "mcp-server/src/tools/bid.ts"
      to: "mcp-server/src/lib/proof-generator.ts"
      via: "auto proof generation when AGENT_STATE_FILE set"
      pattern: "generateBidRangeProof"
    - from: "mcp-server/src/lib/identity-check.ts"
      to: "engine /verify-identity"
      via: "expanded pre-flight: identity + ZK readiness"
      pattern: "verifyIdentityPreFlight"
---

<objective>
Make ZK proofs mandatory and unify readiness checks across engine and MCP tools.

Purpose: Enforce that all agents must prove registry membership and bid range via ZK proofs -- no opt-out path when agent state is configured. This phase enforces what Phase 8 assumed.
Output: Engine defaults to requiring proofs, MCP tools auto-generate proofs, check_identity returns single readiness flag, .env.example updated.
</objective>

<execution_context>
@/Users/zengy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-zk-enforcement/09-CONTEXT.md

Key source files:
@engine/src/auction-room.ts (line 504: requireProofs default flip)
@engine/src/handlers/actions.ts (lines 264-270, 320-325, 420-423: proof enforcement)
@mcp-server/src/tools/join.ts (lines 57-137: proof generation logic)
@mcp-server/src/tools/bid.ts (lines 64-200: proof generation logic)
@mcp-server/src/tools/identity.ts (lines 102-106: readiness fields)
@mcp-server/src/lib/identity-check.ts (verifyIdentityPreFlight)
@mcp-server/src/lib/proof-generator.ts (existing proof gen functions)
@mcp-server/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Engine default flip + MCP tool ZK enforcement</name>
  <files>
    engine/src/auction-room.ts
    engine/src/handlers/actions.ts
    mcp-server/src/tools/join.ts
    mcp-server/src/tools/bid.ts
    mcp-server/src/lib/identity-check.ts
  </files>
  <action>
**Engine default flip (2 files):**

1. In `engine/src/auction-room.ts` line ~504, change:
   `requireProofs: this.env.ENGINE_REQUIRE_PROOFS === 'true'`
   to:
   `requireProofs: this.env.ENGINE_REQUIRE_PROOFS !== 'false'`
   This makes ENGINE_REQUIRE_PROOFS default to true when unset (Phase 7 VERIFY_WALLET pattern).

2. In `engine/src/handlers/actions.ts`, ensure the structured error when proof is missing returns:
   `{ error: 'PROOF_REQUIRED', detail: 'ZK proof is mandatory for this action', suggestion: 'Generate proof using AGENT_STATE_FILE or provide proofPayload' }`
   Follow the existing error pattern. Check both JOIN (lines ~264-270) and BID (lines ~420-423) enforcement paths.

**MCP join_auction (mcp-server/src/tools/join.ts):**

3. Remove the `generateProof` parameter from the Zod schema entirely.
4. Keep `proofPayload` as optional override, update its description to: "Advanced: pre-built proof override. Omit to auto-generate."
5. Update the tool description: remove mention of `generateProof: true`.
6. Change the execution logic: when `proofPayload` is NOT provided, check if `process.env.AGENT_STATE_FILE` exists. If yes, auto-generate the membership proof using the existing `proof-generator.ts` functions. If AGENT_STATE_FILE is not set and no proofPayload: return hard error `{ code: 'ZK_STATE_REQUIRED', detail: 'AGENT_STATE_FILE not configured and no proofPayload provided', suggestion: 'Set AGENT_STATE_FILE to your agent-N.json path, or provide a pre-built proofPayload' }`.
7. Update the function signature to remove `generateProof` from destructuring.

**MCP place_bid (mcp-server/src/tools/bid.ts):**

8. Same pattern as join: remove `generateProof` param, keep `proofPayload` as advanced override, auto-generate when AGENT_STATE_FILE set, hard error when neither available.
9. For sealed bids: sealed mode should auto-activate when proof is generated (bid range proof implies sealed commitment). Keep existing salt logic.

**MCP pre-flight expansion (mcp-server/src/lib/identity-check.ts):**

10. Expand `verifyIdentityPreFlight()` or create a new `verifyParticipationReadiness()` that checks: (1) identity via /verify-identity, (2) AGENT_STATE_FILE exists locally, (3) agent is registered on privacy registry. Fail fast with clear messages before expensive proof generation.
11. Call this expanded pre-flight from both join and bid tools BEFORE proof generation.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck && cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npx tsc --noEmit</automated>
  </verify>
  <done>
- ENGINE_REQUIRE_PROOFS defaults to true (!== 'false' pattern)
- join_auction has no generateProof param; auto-generates proof when AGENT_STATE_FILE set
- place_bid has no generateProof param; auto-generates proof when AGENT_STATE_FILE set
- Both tools return ZK_STATE_REQUIRED error when no proof source available
- Pre-flight checks identity + ZK readiness before proof generation
- proofPayload retained as advanced override in both tools
  </done>
</task>

<task type="auto">
  <name>Task 2: Unify check_identity readiness + update .env.example</name>
  <files>
    mcp-server/src/tools/identity.ts
    mcp-server/.env.example
  </files>
  <action>
**check_identity readiness merge (mcp-server/src/tools/identity.ts):**

1. At lines ~105-106, merge the two fields into one:
   - `readyToParticipate` = `data.verified AND data.privacyRegistered`
   - Remove `readyForZkProofs` field entirely from the response (not kept as alias, not deprecated -- deleted)
2. Update the tool's output description to document that `readyToParticipate` means BOTH ERC-8004 identity verified AND privacy registry membership confirmed.
3. Ensure the response still includes the individual detail fields (`erc8004Registered`, `privacyRegistered` etc.) for diagnostic purposes, but the top-level actionable flag is just `readyToParticipate`.
4. AGENT_STATE_FILE is a local config concern -- do NOT check it in check_identity (remote-only check per user decision).

**.env.example update (mcp-server/.env.example):**

5. Reorganize the file with a clear `# Required` section at the top grouping:
   - AGENT_PRIVATE_KEY
   - AGENT_ID
   - AGENT_STATE_FILE (with comment: "Path to agent-N.json — needed for mandatory ZK proof generation")
   - BASE_SEPOLIA_RPC (with comment: "RPC endpoint for Base Sepolia — needed for on-chain identity verification and proof generation")
6. Move optional/advanced vars to a separate `# Optional` section below.
7. Remove any mention of `generateProof: true` from comments (it no longer exists).
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/mcp-server && npx tsc --noEmit && grep -c "readyForZkProofs" src/tools/identity.ts | grep -q "^0$" && echo "readyForZkProofs removed" && grep "# Required" .env.example && echo "env.example has Required section"</automated>
  </verify>
  <done>
- check_identity returns single readyToParticipate = (erc8004Registered AND privacyRegistered)
- readyForZkProofs field completely removed from response
- .env.example has Required section at top with AGENT_STATE_FILE, BASE_SEPOLIA_RPC, AGENT_PRIVATE_KEY, AGENT_ID
- .env.example explains these are needed for mandatory ZK proof generation
  </done>
</task>

</tasks>

<verification>
1. `cd engine && npm run typecheck` passes
2. `cd mcp-server && npx tsc --noEmit` passes
3. `grep "!== 'false'" engine/src/auction-room.ts` shows the default flip
4. `grep -c "generateProof" mcp-server/src/tools/join.ts` returns 0 (param removed)
5. `grep -c "generateProof" mcp-server/src/tools/bid.ts` returns 0 (param removed)
6. `grep -c "readyForZkProofs" mcp-server/src/tools/identity.ts` returns 0 (field removed)
7. `grep "# Required" mcp-server/.env.example` shows required section exists
</verification>

<success_criteria>
- Engine requires ZK proofs by default (ENGINE_REQUIRE_PROOFS defaults true)
- MCP join/bid tools auto-generate proofs when AGENT_STATE_FILE is set
- No opt-out parameter exists (generateProof removed)
- check_identity returns unified readyToParticipate flag
- .env.example documents required fields clearly
</success_criteria>

<output>
After completion, create `.planning/phases/09-zk-enforcement/09-01-SUMMARY.md`
</output>
