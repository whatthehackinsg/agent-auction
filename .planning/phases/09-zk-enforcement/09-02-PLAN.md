---
phase: 09-zk-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - engine/src/test-helpers/proof-fixtures.ts
  - engine/src/handlers/__tests__/actions.test.ts
  - engine/src/handlers/__tests__/join-proof.test.ts
  - engine/src/handlers/__tests__/bid-proof.test.ts
autonomous: true
requirements: [ZKRQ-01, ZKRQ-02]

must_haves:
  truths:
    - "Engine tests use real Groth16 proofs, not ALLOW_INSECURE_STUBS bypass"
    - "JOIN without proof is rejected by engine when ENGINE_REQUIRE_PROOFS unset"
    - "BID without proof is rejected by engine when ENGINE_REQUIRE_PROOFS unset"
    - "JOIN with valid Groth16 proof is accepted"
    - "BID with valid Groth16 proof is accepted"
  artifacts:
    - path: "engine/src/test-helpers/proof-fixtures.ts"
      provides: "Real Groth16 proof generation for tests"
      exports: ["generateTestMembershipProof", "generateTestBidRangeProof"]
    - path: "engine/src/handlers/__tests__/actions.test.ts"
      provides: "Updated action tests with real proofs"
  key_links:
    - from: "engine/src/test-helpers/proof-fixtures.ts"
      to: "engine/src/handlers/actions.ts"
      via: "proof fixtures used in test assertions"
      pattern: "generateTest.*Proof"
    - from: "engine/src/handlers/actions.ts"
      to: "snarkjs groth16 verify"
      via: "server-side proof validation"
      pattern: "groth16\\.verify"
---

<objective>
Update engine tests to use real Groth16 proofs instead of relying on ALLOW_INSECURE_STUBS bypass.

Purpose: With ENGINE_REQUIRE_PROOFS now defaulting to true (Plan 01), tests must generate and submit real ZK proofs. This proves the enforcement actually works end-to-end.
Output: Test helper for proof generation, updated engine tests verifying proof enforcement with real Groth16 proofs.
</objective>

<execution_context>
@/Users/zengy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zengy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-zk-enforcement/09-CONTEXT.md
@.planning/phases/09-zk-enforcement/09-01-SUMMARY.md (for default flip details)

Key source files:
@engine/src/handlers/actions.ts (proof verification logic)
@engine/src/handlers/__tests__/actions.test.ts (existing tests)
@packages/crypto/src/index.ts (proof generation functions)
@engine/src/crypto.ts (engine-side crypto: verify, vkeys)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test proof fixture helper</name>
  <files>
    engine/src/test-helpers/proof-fixtures.ts
  </files>
  <behavior>
    - generateTestMembershipProof(agentId, treeIndex, registryRoot) returns valid Groth16 proof object accepted by engine verifier
    - generateTestBidRangeProof(agentId, amount, minBid, maxBid) returns valid Groth16 proof object accepted by engine verifier
    - Generated proofs pass snarkjs.groth16.verify with the inlined vkeys in engine/src/crypto.ts
  </behavior>
  <action>
Create `engine/src/test-helpers/proof-fixtures.ts` that exports two functions for generating real Groth16 proofs for tests.

1. Examine how `packages/crypto` generates proofs (look at `generateMembershipProof` and `generateBidRangeProof` exports). Replicate or import the same approach.
2. The engine uses snarkjs.groth16.verify with inlined vkeys (see engine/src/crypto.ts). The test proofs MUST pass this verification.
3. Use the actual circuit WASM and zkey files from `circuits/` directory. Find their paths by checking how packages/crypto or agent-client references them.
4. Export `generateTestMembershipProof` and `generateTestBidRangeProof` as async functions.
5. Include a `setupTestProofs()` that can be called in `beforeAll` to warm up snarkjs (accept 10-20s startup time per user decision).
6. These are real proofs -- not mocks, not stubs. They must survive `groth16.verify()`.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run typecheck</automated>
  </verify>
  <done>
- proof-fixtures.ts exports generateTestMembershipProof and generateTestBidRangeProof
- Functions produce real Groth16 proofs using actual circuit files
- TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Update engine tests to use real proofs</name>
  <files>
    engine/src/handlers/__tests__/actions.test.ts
    engine/src/handlers/__tests__/join-proof.test.ts
    engine/src/handlers/__tests__/bid-proof.test.ts
  </files>
  <action>
1. Find all existing engine tests that test JOIN and BID actions. Check if they currently set `ENGINE_ALLOW_INSECURE_STUBS=true` to bypass proof checks.

2. For the main actions test file, update tests that submit JOIN/BID to either:
   - Include real Groth16 proofs from the fixture helper (for tests that should pass)
   - Remove ALLOW_INSECURE_STUBS and verify rejection when no proof provided (for enforcement tests)

3. Create or update dedicated proof enforcement test files:
   - `join-proof.test.ts`: Tests that JOIN without proof returns PROOF_REQUIRED error when ENGINE_REQUIRE_PROOFS is unset (default true). Tests that JOIN with valid membership proof succeeds.
   - `bid-proof.test.ts`: Tests that BID without proof returns PROOF_REQUIRED error. Tests that BID with valid bid range proof succeeds.

4. Use `beforeAll` with the proof fixture `setupTestProofs()` to generate proofs once per test suite (accept 10-20s startup).

5. Tests that are NOT about proof enforcement can still use `ENGINE_ALLOW_INSECURE_STUBS=true` to focus on their own concern (e.g., testing bid sequencing). But tests specifically about proof paths MUST use real proofs.

6. Run the full engine test suite and ensure it passes. Fix any tests broken by the default flip.
  </action>
  <verify>
    <automated>cd /Volumes/MainSSD/HomeData/zengy/workspace/auction-design/engine && npm run test</automated>
  </verify>
  <done>
- Engine test suite passes with ENGINE_REQUIRE_PROOFS defaulting to true
- JOIN enforcement tested: no proof = PROOF_REQUIRED error, valid proof = accepted
- BID enforcement tested: no proof = PROOF_REQUIRED error, valid proof = accepted
- Non-proof tests still pass (using ALLOW_INSECURE_STUBS where needed)
- No test relies on proof bypass for proof-specific behavior
  </done>
</task>

</tasks>

<verification>
1. `cd engine && npm run test` -- all tests pass
2. `cd engine && npm run typecheck` -- no type errors
3. Proof enforcement tests exist and exercise real Groth16 verify path
4. No test that specifically validates proof behavior uses ALLOW_INSECURE_STUBS
</verification>

<success_criteria>
- Engine tests prove that ZK enforcement works with real Groth16 proofs
- Default-true ENGINE_REQUIRE_PROOFS does not break any existing tests
- Both JOIN and BID proof paths have dedicated test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/09-zk-enforcement/09-02-SUMMARY.md`
</output>
