{
  "name": "auction-settlement-cre",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun build ./workflows/settlement.ts --outdir ./dist"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "latest",
    "viem": "^2.0.0",
    "zod": "^3.0.0"
  }
}
